# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## Unreleased

### Added

### Changed

### Deprecated

### Removed

### Fixed

### Security

## [v1.3.0](https://github.com/vig-os/org-config/releases/tag/v1.3.0) - 2026-08-24

### Added

- **Apply-dispatch reminder: a merged-but-unapplied config no longer goes unnoticed** ([#202](https://github.com/vig-os/org-config/issues/202)): a new reusable `.github/workflows/apply-reminder.yml` comments the pending apply — merge commit, PR link, dispatch command — onto a long-standing, pinned "Apply dispatch tracker" issue and labels it `pending-apply`; the reusable `apply` workflow gains an optional `tracker_issue` input that, on a successful apply, posts the "applied" confirmation on the same thread and clears the label. The gap it closes is structural and downstream-only: on a **private** config repo below Enterprise, environment required reviewers are unavailable (adding one returns HTTP 422), so the engine's apply-on-merge-with-approval model degrades to `workflow_dispatch`-only and the dispatch *is* the approval gate — after which merged-but-unapplied is a **silent** state, since GitHub suppresses self-activity email for the admin who merged and the first signal would be the next scheduled drift run, up to a day later. The tracker's **assignees are the notification list** (assignment mails them on every later bot comment regardless of watch settings, and the list changes without touching a workflow), one long-lived issue avoids per-merge issue churn, and the thread ends up a durable audit record pairing every config merge with the run that applied it — with the daily drift run still the enforcement backstop for an ignored reminder. Deliberate details: the caller filters on **all four** paths apply consumes (`otterdog.json`, `otterdog/**`, `secrets/**`, `.sops.yaml`), because a two-path filter would let a SOPS secret rotation merge with no reminder; the PR link is parsed from the merge commit title's `(#N)` suffix that house policy guarantees, degrading to a commit-only comment on a direct push rather than failing; the workflow takes its **own** `apply-reminder-<org>` concurrency group, never the shared `otterdog-mutate-<org>` one, where a reminder would queue uselessly behind a 30-minute apply; and it needs no secrets, no checkout and no third-party action — the comment and label are same-repo issue writes on the workflow `GITHUB_TOKEN` (`issues: write` + `contents: read`, the latter per [#173](https://github.com/vig-os/org-config/issues/173)). The `apply` side is **not** on `GITHUB_TOKEN`: the [#176](https://github.com/vig-os/org-config/issues/176) standing rule caps that workflow's whole job graph at `contents: read` at assembly time, before any `if:`, so `issues: write` there would startup-fail every existing consumer — the confirmation rides a narrowed App token minted in-workflow (drift.yml's issue-ops pattern), needing no caller change and no new App grant. Its guard, `inputs.tracker_issue != '' && steps.apply.outcome != 'skipped' && steps.apply.outputs.rc == '0'`, is deliberately triple: the apply step runs under `set +e` so its outcome is `success` even when otterdog failed (`rc` is the only truth), and a superseded run ([#105](https://github.com/vig-os/org-config/issues/105)) self-skips yet stays green — a weaker guard would announce "applied" for a run that applied nothing. **Optional, active by default in the template**, which ships the reminder caller enabled with a `TODO(tracker)` placeholder; an org opts out by deleting the caller, and with `tracker_issue` empty every run is a logged green no-op and `apply.yml` is byte-identical to before, at assembly time and at runtime. The engine repo itself is unaffected — it is public and its apply runs behind a real `production` reviewer gate — so this is downstream-only machinery, held here rather than vendored in a consumer per the [#169](https://github.com/vig-os/org-config/issues/169) principle. Rollout is not automatic: there is no template-sync machinery and Renovate only bumps pins on existing callers, so each org hand-adds the caller and does the one-time tracker/label/assignee setup, documented as a standard step in the onboarding runbook.

### Changed

- **The template apply caller now documents both gating modes, and defaults to the one a private org can actually enforce** ([#202](https://github.com/vig-os/org-config/issues/202)): `template/.github/workflows/apply.yml` shipped a `push`-to-`main` trigger plus an instruction to create a required-reviewer `production` environment — advice that **422s on a private repo below Enterprise**, where required reviewers do not exist. A template-faithful private org therefore ended up with apply-on-merge and *no* human gate whatsoever, while the environment it was told to create silently gated nothing. The caller now names the two modes explicitly: **Mode B (default, private Team/Free)** ships the `push:` block commented out, so apply runs only on a human dispatch and that dispatch is the gate, paired with the new reminder caller; **Mode A (public repos, Enterprise orgs)** keeps the reviewer-gated apply-on-merge and is a three-step switch spelled out in the header (create the environment, uncomment `push:`, delete the reminder caller). `template/README.md`'s "After the Team upgrade" section and `template/README.repo.md`'s "How changes flow" are reconciled with the same two-mode reality — the latter previously asserted, flatly and falsely for every private consumer, that apply runs "in a required-reviewer `production` environment". The runbook also gains the tracker onboarding steps (create and pin the issue, assign the admins, create the `pending-apply` label, fill the number into both callers) and the opt-out. Documentation and template only — no change to any engine workflow's behaviour for an existing consumer.

#### Dependencies

- Update `ruff` from `==0.16.3` to `==0.16.4` ([#199](https://github.com/vig-os/org-config/pull/199))
- Adopt vigOS devkit 1.11.0 ([#201](https://github.com/vig-os/org-config/pull/201)) — [release notes](https://github.com/vig-os/devkit/releases/tag/1.11.0)

## [v1.2.2](https://github.com/vig-os/org-config/releases/tag/v1.2.2) - 2026-08-22

### Changed

- **`org-config`'s own Main protection no longer requires a human review** ([#195](https://github.com/vig-os/org-config/issues/195)): `required_approving_review_count` drops from 1 to 0 and `requires_code_owner_review` from `true` to an explicit `false`. The repo is solo-maintained, so both halves of the gate were structurally unsatisfiable on a self-authored PR — GitHub will not accept an author's own review, and every `.github/CODEOWNERS` rule names the author — leaving one possible outcome: a routine `#OrganizationAdmin` bypass on every merge. That is worse than no gate at all, because it normalizes bypassing and fills the audit trail with overrides that record nothing; an honest zero is more truthful than a requirement that is always waived. The same pathology diagnosed on devkit in [#115](https://github.com/vig-os/org-config/issues/115) and resolved on `devkit-smoke-test` in [#167](https://github.com/vig-os/org-config/issues/167). The operative controls are untouched and are what actually bind: the required `CI Summary` status check on `main`, `requires_review_thread_resolution`, the bypass-free `Signed commits` ruleset, and above all the `production` environment gate (reviewer `@c-vigo`, `main`-only branch policy) that stops every live apply with the exact-tree plan preview one click from the approve button — so a human still approves each mutation of the live org, however the PR merged. Accepted trade-off, stated plainly: bot-authored PRs (Renovate, the release train) *were* approvable and so the count-1 gate did bind there; at count 0 they may now merge on green CI without review. The blast radius is a merged commit, not an applied one — nothing reaches the live org without the environment approval. `requires_code_owner_review` is declared as an explicit `false` with an inline rationale rather than deleted (the vendored default is already `false`), so the config states the decision. Out of scope: every other repo's rulesets — devkit deliberately keeps count 1, since its bot-authored release PRs make the gate satisfiable and therefore worth keeping.

- **`sync-issues-action` Main protection now requires up-to-date branches** ([#188](https://github.com/vig-os/org-config/issues/188)): `strict: true` joins the ruleset's `required_status_checks` block, so `CI Summary` and `Dist Check` must have run against the merge result rather than a stale base — without it a PR whose checks passed before `main` moved could merge untested as it will actually land. Same principle as [#184](https://github.com/vig-os/org-config/issues/184)'s stale-review dismissal, applied to CI instead of review: the gates must cover the code actually being merged. `commit-action` and devkit have carried `strict` since their rulesets were written, so this removes an accidental difference between sibling repos rather than inventing a policy; it was explicitly deferred out of #184 as worth its own decision, and this is that decision. The repo's `allow_update_branch` flips to `true` alongside it, so the **Update branch** button (and auto-merge's auto-update) satisfies the requirement without a manual rebase. Cost: one CI re-run per merge, and only when `main` has moved since the branch last built — rare on a low-traffic, solo-maintained repo with short CI. Also clears the `'up-to-date branches' is disabled` warning on OpenSSF Scorecard alert [sync-issues-action#15](https://github.com/vig-os/sync-issues-action/security/code-scanning/15), which stays open: its remaining warnings (approval count 1, last-push approval off) are deliberate for a solo maintainer.

- **`sync-issues-action` and `devkit-smoke-test`: the inert `requires_code_owner_review` flag is now an explicit `false`** ([#187](https://github.com/vig-os/org-config/issues/187)): three rulesets required code-owner review — `sync-issues-action` Dev and Main, `devkit-smoke-test` Dev — while both repos carry the devkit-seeded `.github/CODEOWNERS` stub whose every rule line is commented out. With no active rule no owner ever matches, so the flag demanded nothing on any PR (verified live on `sync-issues-action` Main ruleset 13401788: `require_code_owner_review: true`, never once enforced). Not a security gap — the 1-approval gate on Main is independent and untouched, as are review-thread resolution, the required status checks and the bypass-free `Signed commits` ruleset — but a flag that reads like a control and does nothing is worse than its absence. The alternative fix, populating the stubs, would recreate the [#115](https://github.com/vig-os/org-config/issues/115) problem devkit already resolved: a single owner who also authors the PRs cannot code-owner-approve his own PR, so the gate becomes unsatisfiable and its only outcome is an `#OrganizationAdmin` bypass. Declared as an explicit `false` with an inline rationale rather than by deleting the line (the vendored default is already `false`), matching devkit's style at Main and Release protection. Out of scope and deliberately unchanged: `org-config`'s own Main protection flag, whose CODEOWNERS has seven active rules and therefore a gate that binds. Found during the [#184](https://github.com/vig-os/org-config/issues/184) evaluation and split out per single-issue scope.

- **The README's stale "rulesets are not machine-writable" known-limitation is replaced by the private-repo ruleset *read* gate** ([#191](https://github.com/vig-os/org-config/issues/191)): the section's one remaining bullet still told operators that any ruleset carrying a numeric `15368:` status-check prefix fails to apply and must be hand-applied via `gh api` ([#69](https://github.com/vig-os/org-config/issues/69), [upstream #695](https://github.com/eclipse-csi/otterdog/issues/695)) — false since the otterdog 1.4.0 pin closed #69 ([#145](https://github.com/vig-os/org-config/issues/145)), confirmed end-to-end the same day ([#148](https://github.com/vig-os/org-config/issues/148)) and again in production on 2026-08-21, when [#186](https://github.com/vig-os/org-config/issues/186)'s ordinary apply wrote `dismiss_stale_reviews_on_push` to live ruleset 13401788 with no manual `gh api` step. The section is kept rather than deleted, now carrying what is actually still open at the pinned version: otterdog's `github_organization.py` reads live repo rulesets only for public repos or `enterprise`-plan orgs — stricter than the GitHub API, which supports repo rulesets on Team — so a private downstream org on Team (`exo-pet`) that declares one gets a permanent phantom `add` in every plan and an unsafe duplicate `POST` on apply, and manages those rulesets by hand with the declaration held out of the jsonnet ([#107](https://github.com/vig-os/org-config/issues/107), [upstream #729](https://github.com/eclipse-csi/otterdog/issues/729)). Documentation only — no engine, config, or workflow change.

- **CodeQL default setup is suppressed org-wide; advanced `codeql.yml` is the one scanning mechanism** ([#189](https://github.com/vig-os/org-config/issues/189)): the three `code_scanning_default_setup_enabled: true` declarations (tessera, vigos-mvp, vs-dolt) and both `code_scanning_default_languages` lists are gone from the committed config, converging the stragglers on what the other 7 scanned repos already do — versioned, SHA-pinned, merge-gating CodeQL workflows. The enforced org code-security configuration 237480, which actually pins default-setup enablement and which otterdog cannot model, had its `code_scanning_default_setup` flipped to `disabled` out-of-band (2026-08-21) and is now asserted by a new `unmanaged-controls.toml` row, so a UI re-enable raises a drift issue instead of silently re-arming default setup. Retired with it: the vs-dolt `[[expected]]` allow-list entry, the `plan.yml` benign-drift footnote, and the README known-limitation — all three existed only to paper over default-setup drift ([upstream #694](https://github.com/eclipse-csi/otterdog/issues/694)); a clean plan now reads all zeros. Coverage after this change, explicitly: tessera unscanned until [vig-os/tessera#410](https://github.com/vig-os/tessera/issues/410) adds its `actions`-leg workflow, vs-dolt unscanned by decision, vigos-mvp loses nothing (no CodeQL-supported code).

- **`sync-issues-action` Main protection now dismisses stale reviews on push** ([#184](https://github.com/vig-os/org-config/issues/184)): `dismisses_stale_reviews: true` joins the `required_pull_request` block, so an approval must cover the code actually being merged — without it, a review of one commit survives every later push and the 1-approval gate can be satisfied by a commit nobody reviewed. Straight convergence on the org decision devkit already records ([#118](https://github.com/vig-os/org-config/issues/118)); the two action repos no longer differ here by accident. Main only — Dev and Release require 0 approvals, so they have nothing to dismiss. Everything else in the ruleset is unchanged; deliberately **not** included: `requires_last_push_approval` (self-merge-blocking for a solo maintainer, declined for devkit under #118) and `strict` up-to-date branches (worth its own decision — `commit-action` and devkit carry it, this repo does not). Prompted by OpenSSF Scorecard alert [sync-issues-action#15](https://github.com/vig-os/sync-issues-action/security/code-scanning/15), which this improves but intentionally does not close: its remaining warnings (approval count 1, last-push approval off, up-to-date branches off) are deliberate.

#### Dependencies

- Update `astral-sh/setup-uv` from `v10.0.0` to `v10.0.1` ([#177](https://github.com/vig-os/org-config/pull/177))
- Update `ruff` from `==0.16.2` to `==0.16.3` ([#178](https://github.com/vig-os/org-config/pull/178))
- Lock file maintenance (pip) ([#179](https://github.com/vig-os/org-config/pull/179))
- Adopt vigOS devkit 1.10.0 ([#183](https://github.com/vig-os/org-config/pull/183)) — [release notes](https://github.com/vig-os/devkit/releases/tag/1.10.0)

### Fixed

- **The reusable `apply` workflow no longer exceeds a `contents: read` caller grant — downstream applies start again** ([#176](https://github.com/vig-os/org-config/issues/176)): GitHub validates the caller-permission cap over **every** job in a called workflow's graph at assembly time, **before** any `if:` is evaluated. So the engine-only `preview` job that [#105](https://github.com/vig-os/org-config/issues/105) added inside `apply.yml` — gated `if: inputs.org_github_id == ''` but declaring `pull-requests: write` — was counted against the cap on every downstream call, and each consumer scaffolded from `template/.github/workflows/apply.yml` (which grants exactly `contents: read`, the honest grant for a workflow that only reads the committed config) died with `startup_failure` and zero jobs. Every template-scaffolded consumer was broken from `v1.1.0` onward; found by exo-pet on a `v1.2.1` pin. The preview moves to a new engine-only driver, `.github/workflows/apply-engine.yml`, which now carries this repo's `push`/`workflow_dispatch` triggers and the literal cross-workflow `otterdog-mutate` concurrency group, and runs `preview` and `apply` as two jobs of one run — so the approver still sees the exact-tree plan in the same run's summary, right next to the approve button, exactly as #105 intended. `apply.yml` is now `workflow_call`-only with a single `contents: read` job, and since the engine's own applies go through the same cap algorithm as a pinned downstream call, every engine apply live-tests the downstream contract. `template/` is unchanged apart from a clarifying comment: **no caller edit is needed** — consumers that added an interim `pull-requests: write` to work around this can drop it once they bump their pin.

## [v1.2.1](https://github.com/vig-os/org-config/releases/tag/v1.2.1) - 2026-08-14

### Fixed

- **The reusable `drift` workflow can now check out a private downstream caller** ([#173](https://github.com/vig-os/org-config/issues/173)): the drift job declared `permissions: {}`, so the caller-checkout step failed with `fatal: repository not found` on every private consumer — a zero-scope `GITHUB_TOKEN` cannot read the private repo it runs in, and every downstream org-config repo is private by design (ADR-0006). The engine repo never hit it because it is public. Found by the first real downstream run (exo-pet, pinned at v1.2.0) immediately after [#169](https://github.com/vig-os/org-config/issues/169) unblocked the drift leg. The job now grants `contents: read` — matching the reusable `plan`/`apply` jobs and the header's read-only security model; org reads and issue writes stay exclusively on the two App tokens.

## [v1.2.0](https://github.com/vig-os/org-config/releases/tag/v1.2.0) - 2026-08-14

### Changed

- **`devkit-smoke-test` Main protection no longer requires a human review** ([#167](https://github.com/vig-os/org-config/issues/167)): `required_approving_review_count` drops from 1 to 0. The requirement existed only so the dispatch listener's final-release gate could poll `reviewDecision`, which GitHub computes solely when the base branch requires reviews ([vig-os/devkit#1391](https://github.com/vig-os/devkit/issues/1391)) — a workaround, not a control: the release PR it guarded is entirely bot-authored scaffolding, so the approval carried no information. That gate is removed in devkit by [vig-os/devkit#1506](https://github.com/vig-os/devkit/issues/1506), part of the single-approval release train ([vig-os/devkit#1504](https://github.com/vig-os/devkit/issues/1504)); the operative controls are the ruleset's required `CI Summary` status check and devkit's published-smoke-release validation at promote, and count-0-with-required-checks matches devkit's own Dev and Release protections. Everything else in the ruleset is unchanged. **Sequencing:** this must be applied together with the devkit-side listener change, both before the next devkit release train — either half alone breaks the train (old poll + count 0 → empty `reviewDecision` → timeout; new listener + count 1 → unapprovable merge).

### Fixed

- **The reusable `drift` workflow is now self-contained for downstream callers** ([#169](https://github.com/vig-os/org-config/issues/169)): the reconcile step previously ran `uv run drift-layer` from the **caller's** checkout, but a downstream org-config repo scaffolded from `template/` holds only config data — no `src/drift_layer/`, `pyproject.toml` or `uv.lock` — so no consumer could run drift at all and `template/drift.yml` shipped with a "keep drift disabled" caveat. On downstream runs (`github.repository != 'vig-os/org-config'`) the workflow now performs a second SHA-pinned checkout of this public engine repo into `.drift-engine/`, pinned to `job.workflow_sha` — the commit SHA of the workflow file that defines the current job, which for a reusable-workflow job is the called reusable-workflow file, i.e. automatically the exact SHA the caller pins in `uses:`, so the engine code and workflow can never skew and a Renovate pin bump moves both atomically — and runs the reconciler via `uv run --project`, leaving the working directory (and therefore every data path: committed config, `drift-allowlist.toml`, `unmanaged-controls.toml`) in the caller's checkout. A guard step fails loudly if `job_workflow_sha` is ever empty rather than silently checking out an unpinned engine tree, and the engine checkout happens **after** the plan leg so `otterdog plan` provably runs on an untouched caller tree. The engine's own daily runs skip both new steps (the primary checkout already is the engine tree), keeping own-run semantics byte-identical. `template/drift.yml` now ships enabled, and the "downstream drift needs the drift layer" known-limitation is gone from both READMEs.

## [v1.1.0](https://github.com/vig-os/org-config/releases/tag/v1.1.0) - 2026-08-14

### Added

- **Release-train recipes in `justfile.project`** ([#164](https://github.com/vig-os/org-config/issues/164)): `just changelog-preview`, `prepare-release`, `publish-candidate`, `finalize-release` and `promote-release` replace the commented-out devkit examples, which were both inert and stale — they dispatched from `dev`, a branch this repo deleted when it moved to the trunk model. `prepare-release` now dispatches from `main` and every later step from the `release/X.Y.Z` branch that prepare creates. Two of these are not optional conveniences: `docs/DOWNSTREAM_RELEASE.md` (devkit-managed, so not editable here) already advertises `just changelog-preview` and `just promote-release`, and neither existed. The tag prefix and repository are read from `.vig-os` rather than restated, and the recipe comment records the re-approval step the train needs between `finalize-release` and `promote-release` ([vig-os/devkit#1474](https://github.com/vig-os/devkit/issues/1474)): finalize pushes to the release branch, `Main protection` dismisses stale reviews on push, so the approval that authorized the final dispatch is already gone by the time promote validates it.
- **`DEVKIT_UPGRADE_APP_CLIENT_ID` org secret declared** ([#112](https://github.com/vig-os/org-config/issues/112)): the `vig-os` **organization** secret holding the devkit-upgrade App's client ID is now declared in `otterdog/vig-os/vig-os.jsonnet`, alphabetically beside the existing `DEVKIT_UPGRADE_APP_ID` and matching the `COMMIT_APP_CLIENT_ID` / `RELEASE_APP_CLIENT_ID` shape (dummy `'********'` value, no `visibility` field — `vig-os` org secrets are visibility `all` by convention). The secret was created live on 2026-08-07; this is the config-side half of the pair, so the declaration matches the org rather than drifting from it. Pre-work for phase 4 of the App-secret consolidation program tracked in [#112](https://github.com/vig-os/org-config/issues/112): the devkit release that renames `DEVKIT_UPGRADE_APP_ID` -> `DEVKIT_UPGRADE_APP_CLIENT_ID` in the `devkit-upgrade.yml` scaffold is [vig-os/devkit#1365](https://github.com/vig-os/devkit/issues/1365). Both secrets coexist until every consumer is re-scaffolded; the numeric `DEVKIT_UPGRADE_APP_ID` is retired in a later phase.
- **Unmanaged-controls assertion table** ([#116](https://github.com/vig-os/org-config/issues/116)): a new `src/drift_layer/controls.py` loads `unmanaged-controls.toml`, a declarative table of live org/repo controls otterdog has no schema field for — Actions SHA-pinning, the fork-PR approval policy, the new-repository security defaults, the secret-scanning extras — which are consequently invisible to **both** the plan and the inventory sweep and can be flipped in the UI with nothing noticing. Each `[[control]]` row names a stable `key`, a `scope` (`org`, or `repo` plus a `repository`), a `{org}`/`{repo}`-templated `endpoint`, a dotted `path` into the decoded JSON response, and the `expect`ed literal; optional `tolerated` declares a second accepted value for a known, tracked divergence, so the table records the **desired** value even where reality is currently allowed to differ and the row goes green by itself once the divergence is fixed (any *third* value is real drift). An optional `kind = "unassertable"` documents a control with no readable endpoint. Field selection is a dotted-key walk rather than a jq expression, preserving the drift layer's zero-runtime-dependency, `uvx`-light posture (ADR-0005); `false` and `null` are returned verbatim while an absent field yields a distinct sentinel, so a renamed field degrades instead of fabricating a security finding, and rows that cannot be evaluated as written are dropped rather than half-asserted. Identity is the TOML key, never the live value: the namespaced `unmanaged-control:<scope>:<key>` fingerprint resource is a sibling of the plan's block headers and the sweep's `repository-inventory:`, so all three populations share one dedup keyspace without ever colliding and ADR-0002's update-not-duplicate issue lifecycle applies unchanged.
- **Typed GitHub API errors and a generic read surface on the drift client** ([#116](https://github.com/vig-os/org-config/issues/116)): `RestGitHubClient` gains `get_json(path)` plus the two paginated org-secret reads (`list_org_secrets`, `list_org_secret_repositories`), and every transport failure now surfaces as an `ApiError` carrying the HTTP status — `404` unassertable, `401`/`403` token scope, and `0` for a request that never got an answer at all (DNS, TLS, connection, or decode failure). The typed status is what makes per-row degradation possible for the unmanaged-controls leg: without it the evaluator cannot tell "this control is wrong" from "this control could not be read", and a transient 403 would either be reported as a critical security finding or, worse, silently close a real open one. `get_json` is deliberately a dumb transport with the row's dotted path doing all field selection, so a new control needs a table row rather than a new client method; secret *values* are never returned by the API, so only the metadata the assertion families compare is ever fetched. Existing callers are unaffected in behaviour — the CLI already degrades on any exception from the live-read leg.
- **Unmanaged-control evaluation with per-row degradation** ([#116](https://github.com/vig-os/org-config/issues/116)): `evaluate_controls()` asserts every table row against live state through the injected client and sorts each into exactly one bucket — matched (clean), matched the `tolerated` value (clean plus a summary note, no issue), diverged (a `drift`+`critical` record whose evidence block carries endpoint, path, expected, tolerated, actual, rationale and refs), or unreadable (unresolved). Splitting *clean* from *unresolved* is the load-bearing design point: `reconcile()` closes every open issue whose fingerprint is absent from the records it is handed, so reporting an unreadable row as clean would **phantom-resolve a genuine security finding** on a transient 403. Because a row's fingerprint derives from its TOML key without touching the API, the unresolved rows' issues can be withheld from the reconcile input individually — one unreadable row degrades only itself, strictly better than the inventory sweep's all-or-nothing skip. A `200` whose schema no longer carries the asserted path is likewise unresolved rather than drift, since treating a renamed field as a divergence would fabricate a critical issue out of a GitHub API change. Comparison is strict (`1` is not `true`), responses are memoized per resolved endpoint so the six rows reading `GET /orgs/{org}` cost one request, and `kind = "unassertable"` rows never touch the client at all.
- **Org-secret assertions derived from the committed config** ([#116](https://github.com/vig-os/org-config/issues/116)): `extract_declared_org_secrets()` reads every committed `orgs.newOrgSecret(...)` declaration — name, visibility and `selected_repositories` — with the same strict-literal doctrine already used for `newRepo` (comments stripped, block read to its matching brace, `newRepoSecret(` never matched), and three generated assertion families compare live metadata against it: visibility, reader lists, and undeclared live secrets. This is the one class of drift otterdog structurally *cannot* catch: nine of the ten committed org secrets carry a `'********'` dummy value, so `include_for_live_patch` is false and the plan skips them entirely — a visibility widened in the UI or a reader list hand-edited via `PUT .../repositories` produces no diff at all. otterdog's `public` is translated to the REST API's `all` on read, exactly as its own model does, so an org-wide secret is not reported as permanent drift. Each family yields at most **one** aggregate issue whose body lists every offender (stable identity, volatile body — precisely what the update-in-place lifecycle is for) rather than one issue per secret, and each degrades independently: an unreadable reader list leaves the visibility and inventory families reporting normally. `assert_no_undeclared` is on by default because a live org secret with no committed declaration is an unreviewed credential with no record of who created it or when it should rotate.
- **Unmanaged-control findings reconciled as a third issue population** ([#116](https://github.com/vig-os/org-config/issues/116)): assertion failures now flow into the same ADR-0002 lifecycle as plan drift and the inventory sweep — one deduplicated issue per control, updated on recurrence, auto-closed on resolution — under an `unmanaged-control` label beside `drift`+`critical`. `reconcile_populations()` becomes three-way, and the plan partition now excludes **both** extra labels: without that, the first run would close every inventory and control issue as phantom-resolved. The controls leg degrades one step finer than the sweep's all-or-nothing skip — the issues of rows that could not be *read* are withheld from its reconcile input individually, so a single 403 leaves exactly one issue untouched while every readable row still reports. Issue wording is corrected for the new evidence type: a control issue says the live control diverges from the value asserted in `unmanaged-controls.toml` and labels its evidence block "Live API assertion", rather than claiming a divergence from the committed otterdog config — there is no jsonnet field, and saying otherwise would send the triager hunting for a setting that does not exist; the auto-close comment cites the live assertion for the same reason. `--dry-run` skips the leg (it is a live read by definition) and a new read-only `--controls-report` prints each row's outcome — `OK`/`TOLERATED`/`DRIFT`/`UNRESOLVED`/`SKIPPED`, expected versus actual — with no issue writes at all, which is how a row is verified green *before* the scheduled run would turn it into a critical issue.
- **`unmanaged-controls.toml` seeded with the twelve known unmodellable controls** ([#116](https://github.com/vig-os/org-config/issues/116)): the shipped table now asserts Actions SHA-pinning (org plus `devkit`), the fork-PR approval policy (org plus `devkit`), the two stuck `devkit` secret-scanning extras, the six org-level new-repository security defaults, and the three generated org-secret families; a `template/` stub ships beside it for downstream orgs, and the `unmanaged-control` label joins `.github/label-taxonomy.local.toml`. Every `expect` was verified against live `vig-os` on the seed date and the table lands **green** — `--controls-report` reads 12 `OK` and 3 `TOLERATED`, 0 `DRIFT`, 0 `UNRESOLVED` — so this is a detector, not a posture change; the fork-PR rows lock in today's `first_time_contributors` rather than hardening to `all_external_contributors`, which stays a separate decision. Three rows deliberately record the **desired** value with a `tolerated` escape rather than asserting reality: org SHA-pinning (`expect = true`, tolerated `false` pending [#120](https://github.com/vig-os/org-config/issues/120), so the row goes green with no edit when #120 lands) and the two `devkit` secret-scanning extras, which the enforced org code-security configuration 237480 declares enabled but which need a GHAS Secret Protection entitlement the plan does not carry — a repo-level `PATCH` returns 200 OK and changes nothing. Tolerating those here rather than in `drift-allowlist.toml` is deliberate: an allow-list entry silences the control unconditionally, hides a third value, splits one control's truth across two files, and would be cleared by `apply_allowlist`'s `with_expected()`. `new-repo-advanced-security` is asserted **off** for the same entitlement reason — a default-on would produce new repositories that look protected and are not.
- **The scheduled drift run asserts the unmanaged live controls** ([#116](https://github.com/vig-os/org-config/issues/116)): `drift.yml`'s reconcile step now passes `--unmanaged-controls unmanaged-controls.toml`, so the daily run reconciles three populations in one invocation — plan drift, the inventory sweep, and the live control assertions — each under its own label and its own degradation contract. **No token, permission or App-grant change is needed**, and the header records why: every endpoint the leg touches is a `GET`, and the permissions they need (Org Administration read, Org Secrets read, Repo Administration read) are already inside the full plan token's grant, so the workflow's "read-only by behaviour" argument extends unchanged. The step stays green on a degraded leg without a blanket `continue-on-error` that would also mask a genuine reconcile failure: rows that could not be read surface as `::warning::` annotations while their issues are left untouched. The reconciler's output is teed to a file and fenced into the run summary alongside the plan — the actions taken plus the per-row control annotations — which keeps the CLI free of any GitHub Actions-specific behaviour.
- **`devkit` tag-protection ruleset** ([#115](https://github.com/vig-os/org-config/issues/115)): `devkit` publishes the release tags every consumer pins yet declared no `target: 'tag'` ruleset, so anyone with write access could create, move, or delete one out of band — `commit-action` has had this ruleset since #43. The new `Tag protection` block mirrors it verbatim (`include_refs: ['~ALL']`, creations/updates/deletions blocked, `allows_force_pushes: true`, single bypass actor `vig-os-release-app`). One always-bypass actor is sufficient because `release.yml`'s publish job is the repo's **only** tag writer (`git tag -a` + `git push origin <version>`, on a token minted from `RELEASE_APP_CLIENT_ID`/`RELEASE_APP_PRIVATE_KEY` — App `vig-os-release-app`, `app_id` 2930017) and `promote-release.yml` prunes leftover RC tags with the same App; `devkit` also uses bare `X.Y.Z` tags (no `DEVKIT_TAG_PREFIX`) and declares no floating tags, so nothing ever force-moves a published tag. This ruleset carries no status checks, so `otterdog apply` can **create** it normally — unlike the two ruleset modifications below, it is not affected by [#69](https://github.com/vig-os/org-config/issues/69).

### Changed

- **ADR-0002's "the plan is the sole drift signal" premise recorded as superseded** ([#116](https://github.com/vig-os/org-config/issues/116)): the README's "Drift is issue-only" section now documents the third population and `unmanaged-controls.toml`, and ADR-0002 gains a dated Corrections entry. The ADR's Context framed the drift layer as a wrapper around *the engine's diff*, silently assuming every governed control is expressible in otterdog's schema — Actions SHA-pinning, the fork-PR approval policy, the six org-level new-repository security defaults and org-secret metadata are not, so until [#116](https://github.com/vig-os/org-config/issues/116) they could be changed in the UI with nothing in the pipeline noticing. What the correction explicitly does **not** change is the decision itself: findings stay issue-only, stay exactly one deduplicated `drift`+`critical` issue per divergence, stay updated-on-recurrence and closed-on-resolution, and the run still needs only read plus issue-write credentials. The `managed: false` philosophy is extended rather than bypassed — a knowingly-wrong control declares a `tolerated` value in its own row, keeping the desired value on record, instead of being silenced in `drift-allowlist.toml`. Because only the evidence source is new and the response policy is untouched, this is a correction to ADR-0002 rather than a new ADR.
- **`devkit-smoke-test`: `#OrganizationAdmin` added as a permanent bypass actor on `Main protection`** ([#147](https://github.com/vig-os/org-config/issues/147)): the ruleset declared no bypass actors, so an org admin had no sanctioned escape hatch on `main` — any operational unblock meant an out-of-band `gh api PUT` on the live ruleset (id 13444875) that the next `otterdog apply` would revert, leaving the override both undeclared and unstable. The always-mode `'#OrganizationAdmin'` bypass matches the one `devkit`'s own `Main protection` has carried since its introduction, so the smoke-test repo now follows the same parity precedent instead of being the stricter of the two. The normal path is unchanged: the 1-approving-review requirement stays (the dispatch listener's final-release gate polls `reviewDecision`, which GitHub only computes when reviews are required — [vig-os/devkit#1391](https://github.com/vig-os/devkit/issues/1391)), and the `Signed commits` ruleset remains bypass-free. Operationally this is also the first real **ruleset modification** since the otterdog 1.4.0 pin ([#69](https://github.com/vig-os/org-config/issues/69)/[#145](https://github.com/vig-os/org-config/issues/145)): the ruleset carries a numeric `15368:` status-check prefix, so its apply doubles as the pending end-to-end confirmation of the upstream write fix; the documented manual-PUT interim remains the fallback if the write still 404s.
- **`devkit`: `requires_code_owner_review` dropped from Main and Release protection** ([#115](https://github.com/vig-os/org-config/issues/115)): both rulesets required code-owner review while `.github/CODEOWNERS` names exactly one owner (`@c-vigo`) on every entry. Since that same account authors the PRs and GitHub will not accept an author's own review, the gate on any CODEOWNERS-covered PR was structurally unsatisfiable and its only possible outcome was an `#OrganizationAdmin` bypass — a control that produces nothing but bypasses is worse than no control, because it normalizes the bypass and pollutes the audit trail. [vig-os/devkit#1219](https://github.com/vig-os/devkit/issues/1219) already reduced CODEOWNERS to the single-maintainer reality; this is the ruleset half. The gates that *do* bind are untouched: 1 approving review on `main`, review-thread resolution, required status checks, and the bypass-free `Signed commits` ruleset. Declared as an explicit `requires_code_owner_review: false` with an inline rationale rather than by deleting the line (the vendored default is already `false`), so the config states the decision instead of leaving it to a default.

#### Dependencies

- Update `actions/upload-artifact` from `v4.6.2` to `v7.0.1` ([#155](https://github.com/vig-os/org-config/pull/155))
- Update `pytest` from `>=8` to `==9.1.1` ([#153](https://github.com/vig-os/org-config/pull/153))
- Update `pytest-cov` from `>=5` to `==7.1.0` ([#153](https://github.com/vig-os/org-config/pull/153))
- Update `ruff` from `>=0.6` to `==0.16.2` ([#153](https://github.com/vig-os/org-config/pull/153), [#160](https://github.com/vig-os/org-config/pull/160))
- Update `actions/checkout` from `v7.0.0` to `v7.0.1` ([#154](https://github.com/vig-os/org-config/pull/154))
- Update `astral-sh/setup-uv` from `v8.3.2` to `v10.0.0` ([#156](https://github.com/vig-os/org-config/pull/156), [#161](https://github.com/vig-os/org-config/pull/161))
- Lock file maintenance (pip) ([#157](https://github.com/vig-os/org-config/pull/157), [#162](https://github.com/vig-os/org-config/pull/162))
- Adopt vigOS devkit 1.9.0 ([#144](https://github.com/vig-os/org-config/pull/144), [#159](https://github.com/vig-os/org-config/pull/159), [#163](https://github.com/vig-os/org-config/pull/163)) — [release notes](https://github.com/vig-os/devkit/releases/tag/1.9.0)

### Removed

- **Per-PR bot changelog workflows `renovate-changelog-build.yml` / `renovate-changelog-commit.yml`** ([#158](https://github.com/vig-os/org-config/issues/158)): both workflows are deleted, along with their `.github/renovate-default.json` `postUpgradeTasks` wiring and their `zizmor.yml` suppressions. They implemented the old per-PR pipeline that appended a changelog line to every bot PR branch; devkit 1.8.0 replaces it with release-time synthesis ([vig-os/devkit#1423](https://github.com/vig-os/devkit/issues/1423)), so the entries are now regenerated from merged bot-PR metadata at release cut and finalize, coalesced to the **net delta per dependency** — intermediate versions that never shipped in a published release no longer appear. This is why Renovate and devkit-adoption PRs merged since the switch carry no hand-written entry: they are not missing, they are synthesized into a `#### Dependencies` block under `### Changed` when the release is cut (preview it read-only with `just changelog-preview`). The four hand-written Renovate entries left over from the retired pipeline were removed from this section for the same reason — they cited superseded intermediate versions (`setup-uv v9.0.0`, `ruff 0.15.22`) that the synthesized block correctly coalesces to `v8.3.2 -> v10.0.0` and `>=0.6 -> ==0.16.2`, and the synthesizer never touches hand-written text, so leaving them would have shipped two contradictory accounts of the same upgrades.
- **Orphaned `DOCKERHUB_TOKEN` / `DOCKERHUB_USERNAME` org secrets** ([#125](https://github.com/vig-os/org-config/issues/125)): the two `vig-os` **organization** secrets are dropped from `otterdog/vig-os/vig-os.jsonnet`, superseding the empty-`selected_repositories` interim from [#123](https://github.com/vig-os/org-config/issues/123). They were never a mistake — they were collateral of the Debian -> Nix migration in `devkit`. `devkit` `36a40550` ("ci: authenticate Docker Hub pulls to avoid rate limits", [vig-os/devkit#473](https://github.com/vig-os/devkit/issues/473), 2026-04-02) added `dockerhub-username`/`dockerhub-token` inputs to the `build-image` composite action and logged in to `docker.io` before `setup-buildx-action` so CI pulls were authenticated rather than anonymous; the org secrets were created the **same day** (11:30 and 11:31 UTC) to feed it. `devkit` `1d4e9db1` ("build(nix): decommission the Debian build path", 2026-06-24, on `main` and `dev`) removed the login along with the path that needed it, and nothing has referenced either name since. The justification is gone too, not merely unused: the Nix path resolves `github:NixOS/nixpkgs` flake inputs, so nothing in the fleet anonymous-pulls from Docker Hub and no rate-limit exposure remains to re-authenticate against. Re-confirmed at retirement per the issue's step 1, against **live default-branch trees rather than the code-search index** (which returned only stale `docs/` hits from the 2026-04 PRs and would have suggested `ci.yml`/`release.yml` still passed the secrets): 231 workflow and composite-action YAML files across all 14 repos, plus every remote branch of `devkit` including the `dev` integration branch — zero references. All five surviving `docker/login-action` steps in `devkit` (`nix-image.yml` x2, `promote-release.yml` x2, `release.yml`) target `ghcr.io` with `github.token`. `vs-dolt`'s `cd-push-docker-image.yaml` is the org's only remaining Docker Hub consumer and reads its **own repo-level** `DOCKER_HUB_USERNAME` / `DOCKER_HUB_ACCESS_TOKEN` (underscore after `DOCKER`, different scope) — deliberately untouched. Unlike the #111 precedent, the live org secrets were deleted by the org owner **before** this change merged, so `plan` shows no pending deletions (`otterdog apply` never deletes secrets, so the order matters only for plan noise). Deleting a GitHub secret does not revoke the credential it held, so the underlying Docker Hub access token — which would otherwise have stayed valid indefinitely with no consumer and no audit trail — was revoked in Docker Hub's own account settings as part of the same retirement. Phase 2 of the orphaned-org-secret retirements begun in [#111](https://github.com/vig-os/org-config/issues/111); found by the consumer-matrix sweep in [#123](https://github.com/vig-os/org-config/issues/123).
- **Orphaned `APP_SYNC_ISSUES_ID` / `APP_SYNC_ISSUES_PRIVATE_KEY` org secrets** ([#111](https://github.com/vig-os/org-config/issues/111)): the two `vig-os` **organization** secrets are dropped from `otterdog/vig-os/vig-os.jsonnet`. A 2026-08-07 sweep of every `sync-issues.yml` in the org found zero consumers at org scope: the only workflows naming them are `tessera`'s, and `tessera` carries its **own repo-level secrets of the same names** belonging to a different App (the repo-scoped `tessera-sync-issues-bot`, `app_id` 4483218) — a repo-level secret always shadows an org-level one, so nothing ever resolved to the org pair. Every other repo's `sync-issues.yml` authenticates with `COMMIT_APP_*`. `tessera`'s `orgs.newRepoSecret('APP_SYNC_ISSUES_*')` declarations are deliberately **untouched** (the comment beside them already warns against "deduplicating" them into org secrets). Because `otterdog apply` never deletes secrets, the live org secrets are deleted out-of-band after merge; until then they surface as two pending deletions in the plan. Phase 1 of the App-secret consolidation program tracked in [#112](https://github.com/vig-os/org-config/issues/112).

### Fixed

- **`apply` can no longer execute a stale approved tree** ([#105](https://github.com/vig-os/org-config/issues/105)): the production-environment approval gates the *run*, not the *tree* — applies queue behind the approval wait (serialized `otterdog-mutate` group), so a later merge to trunk could supersede a still-waiting run, and approving that older run mutated live state toward a config no longer on `main` (the [#99](https://github.com/vig-os/org-config/issues/99) incident: an approved stale tree recreated three intentionally deleted repos as empty stubs plus their rulesets, then exited rc=2 on the post-create patches). Two defenses, combining the first and third guards proposed in #105: **(1) supersession self-guard** — the gated job's first step (i.e. the first thing to run *after* approval, the only moment the check is meaningful) compares the run's `github.sha` against the live tip of the applied branch via the API; on mismatch every subsequent step skips, the job stays green, and a notice + job summary explain that the newer queued apply carries the delta. Consequence, documented in the workflow header: re-running an old run can never apply an old tree — re-applying the *current* state takes a fresh `workflow_dispatch`. **(2) pre-approval plan preview** — a new ungated, read-only `preview` job runs the *exact* tree's plan into the same run's job summary, one click from the approve button, so the reviewer sees what approving will execute instead of trusting the queue. It reuses `plan.yml` via a local `workflow_call` (single source of the plan harness; `plan.yml`'s fork-PR guard now also admits `push`, which is fork-safe by construction), is deliberately not a `needs:` of `apply` (a preview harness failure must never block a legitimate apply), and runs on this repo's own events only — the empty-`inputs` test distinguishes an own push/dispatch from a downstream `workflow_call`, so template callers ([#56](https://github.com/vig-os/org-config/issues/56)) are unaffected and can wire their own preview beside their apply caller. The cancel-superseded-runs variant from #105 was deliberately not taken: it needs `actions: write` on a separate trigger, and the self-guard already makes the superseded run die harmlessly at its own first step.
- **Otterdog bumped 1.3.4 -> 1.4.0 and the eighteen status-check tokens restored to the numeric `15368:` form** ([#69](https://github.com/vig-os/org-config/issues/69)): resolves both horns of the dilemma #69 documented. Upstream [eclipse-csi/otterdog#695](https://github.com/eclipse-csi/otterdog/issues/695) shipped in otterdog **1.4.0** ([PR #700](https://github.com/eclipse-csi/otterdog/pull/700)) as a **write-path-only** fix: a numeric `<integration_id>:<context>` token is now sent as `integration_id` instead of being resolved as an app slug (`GET /apps/15368` -> 404, the failure that broke every ruleset write since #65). The read/render path is untouched — and structurally so: otterdog's id->slug injection only consults `GET /orgs/{org}/installations`, and `github-actions` (app_id 15368) is a first-party app that is never an org installation, so live rulesets *always* read back numeric. The slug migration in [#129](https://github.com/vig-os/org-config/issues/129) (a workaround for the write 404) therefore produced a permanent phantom `15368:X -> github-actions:X` plan diff on all twelve app-bound rulesets, filed by the drift layer as [#130](https://github.com/vig-os/org-config/issues/130)–[#141](https://github.com/vig-os/org-config/issues/141) — this restores the numeric form (undoing `3487ed9`), which zeroes the plan and lets the reconciler auto-close all twelve on the next scheduled run. The canonical prefix form is thus **per-app** (numeric for first-party apps, slug for org-installed ones), recorded in an inline comment in `otterdog/vig-os/vig-os.jsonnet`. The pin is bumped in `justfile.project` (SSoT, ADR-0005) **and** in the four hand-maintained `template/.github/workflows/{plan,apply,drift,import}.yml` literals, which are not wired to the SSoT ([#56](https://github.com/vig-os/org-config/issues/56)); L1 plan fixtures re-checked against 1.4.0 per ADR-0007. The `gh api` manual-PUT interim from #69 stays the fallback until a real ruleset write under 1.4.0 confirms the fix end-to-end.

### Security

- **The one-time `all -> selected` flip executed for the nine dummy-valued org secrets; declared consumer lists refreshed to post-devkit-1.7.0 reality and pre-seeded for the client-ID migration** ([#112](https://github.com/vig-os/org-config/issues/112)): the org owner performed the UI flip on 2026-08-10 (by #123's design the dummy-valued declarations are documentation, so the flip is out-of-band by construction), and this change re-aligns the declared `selected_repositories` lists with what was actually implemented. The lists were re-derived from a same-day sweep of every live default-branch `.github` workflow tree (all org repos, `.yml` and `.yaml`) because the #123 sweep of 2026-08-07 had **gone stale within 72 hours**: the devkit 1.7.0 adoptions re-scaffolded `sync-issues.yml` from `app-id` to `client-id` in `devkit`, `devkit-smoke-test`, and `org-config`, so `COMMIT_APP_ID` narrows by those three repos — its remaining consumers are `commit-action` and `sync-issues-action` (numeric form in their pre-1.7 `sync-issues.yml`) plus the fully-legacy `h5v` and `scitadel`. The same wave made `DEVKIT_UPGRADE_APP_CLIENT_ID` (declared consumer-less in #114) a real consumer secret: `devkit-smoke-test` and `org-config` reference it today, transitionally alongside the numeric form. Deliberately wider than strict references: `h5v` and `scitadel` are **pre-seeded** into all three `*_CLIENT_ID` lists and the corresponding `*_PRIVATE_KEY` unions so their eventual re-scaffold ([#112](https://github.com/vig-os/org-config/issues/112)) starts with working credentials — avoiding the empty-credential-no-error trap the `DEVKIT_UPGRADE` maintenance-coupling comment documents. One transcription error in the UI flip was caught by reading the live lists back through the value-free `GET /orgs/vig-os/actions/secrets/<name>/repositories` endpoint — `RELEASE_APP_CLIENT_ID` briefly granted `nvd-mirror` (adjacent in the repo picker) instead of `scitadel` — and corrected through the same value-free repositories API. That near-miss is the concrete case for asserting these lists mechanically: nothing in this repo would have noticed, which is exactly the [#116](https://github.com/vig-os/org-config/issues/116) drift-assertion gap.
- **The remaining eleven org secrets declared `visibility: selected` with per-secret consumer lists** ([#123](https://github.com/vig-os/org-config/issues/123)): all twelve `vig-os` organization secrets were `visibility: all`, so three GitHub App **private keys** and a Docker Hub token were readable by every workflow in all 14 repos — including unmaintained `vs-dolt` and the automation-churned `org-config-testbed`, neither of which consumes a single org secret. Lists were derived from a full sweep of every `secrets.*` reference in all 14 repos (whole `.github` tree, `.yml` **and** `.yaml` — `vs-dolt` uses the latter and a `*.yml` glob would have missed it; cross-repo `workflow_call` checked and absent, so every `secrets: inherit` resolves in the repo declaring the caller). The lists deliberately differ per secret rather than sharing one: the client-ID migration ([#112](https://github.com/vig-os/org-config/issues/112)) means `*_APP_CLIENT_ID` covers the five repos on `DEVKIT_VERSION=1.6.0` while `*_APP_ID` still covers `h5v` (`0.3.1`) and `scitadel` (`0.3.3`), and the private keys are the union. `DEVKIT_UPGRADE_APP_*` reaches only the four repos actually carrying a scaffolded `devkit-upgrade.yml`; a **maintenance-coupling comment** in the jsonnet records that any repo re-scaffolded to devkit >= 1.6 must be added or its upgrade workflow fails with an empty credential and no error. `DOCKERHUB_TOKEN`/`DOCKERHUB_USERNAME` get an empty list — they are orphaned (zero consumers org-wide; `vs-dolt` uses its own repo-level `DOCKER_HUB_*`), with retirement tracked in [#125](https://github.com/vig-os/org-config/issues/125). **Config-only change, and inert until the values become resolvable:** these eleven are declared `value: '********'`, and `otterdog/models/secret.py:88` excludes dummy-valued secrets from live-patch generation (`models/__init__.py:788`), while the REST API rejects a visibility change without `encrypted_value`+`key_id` (HTTP 422). So the one-time `all -> selected` flip is a UI action by the org owner — and because `plan` renders live patches, these eleven produce **no plan diff at all**, verified empirically. The declared visibility is therefore documentation of the intended state, **not** an assertion: nothing in this repo will notice if a list is widened again. Machine-checking it belongs with the other unmanaged controls in [#116](https://github.com/vig-os/org-config/issues/116); making it genuinely declarative would require moving the eleven values into SOPS as the canary already is, which collides with ADR-0003's deliberate bootstrap exclusions and is out of scope here.
- **`ORG_CONFIG_CANARY` narrowed from org-wide to `org-config` only** ([#123](https://github.com/vig-os/org-config/issues/123)): first secret migrated off `visibility: all`, and the pilot for the remaining eleven. A grep of every `secrets.*` reference in all 14 `vig-os` repos (both `.yml` and `.yaml`, whole `.github` tree, cross-repo `workflow_call` checked and absent) found **no workflow anywhere reads this secret** — it is written by `otterdog apply` from the committed SOPS ciphertext and exists only to prove that pipeline, so `org-config` is the entire audience. It is also the only org secret this repo can migrate declaratively: its jsonnet value is a real credential-provider reference (`pass:org-config/ORG_CONFIG_CANARY`) rather than a `'********'` dummy, so `include_for_live_patch` is true (`otterdog/models/secret.py:88`) and `apply` writes visibility itself. The other eleven are dummy-valued and therefore **skipped** by apply, and the REST API refuses a visibility change without `encrypted_value`+`key_id` (HTTP 422), so their one-time `all -> selected` flip is a UI action — tracked in [#123](https://github.com/vig-os/org-config/issues/123).
- **Workflows can no longer approve pull requests anywhere in `vig-os`** ([#121](https://github.com/vig-os/org-config/issues/121)): the org-level `settings.workflows.actions_can_approve_pull_request_reviews` was `true`, so a GitHub Actions workflow could submit an **approving review** — the one permission that satisfies `Main protection`'s `required_approving_review_count: 1` without a human, reachable by any workflow with `pull-requests: write`, including one introduced by the very PR it would approve. With `allowed_actions: all` and no action allow-list, that reached every repo in the org. `devkit` was the only repo closing it at repo level; it is now `false` org-wide. `devkit`'s repo-level declaration is deliberately left in place — redundant with the new default, but it keeps the intent explicit on the repo that publishes the fleet's tooling. Note the nesting: org-level workflow settings live under `settings.workflows` in the vendored schema's `newOrg`, a different location from the repo-level `workflows+:` block that hangs directly off `newRepo`.
- **Organization members can no longer create repositories** ([#121](https://github.com/vig-os/org-config/issues/121)): `members_can_create_public_repositories` and `members_can_create_private_repositories` flip from an explicit `true` to an explicit `false`. Repository creation in this org is config-first — a repo is declared in `otterdog/vig-os/vig-os.jsonnet` and `otterdog apply` creates it — and a repo created through the UI instead bypasses every default this repo exists to enforce (merge policy, rulesets, signed commits, secret scanning) while starting with no ruleset at all. The recovery path is deliberately asymmetric: apply **creates** what the config declares but never **deletes** what it omits (`--delete-resources` is deliberately omitted, `.github/workflows/apply.yml:87`), so an undeclared repo is never cleaned up automatically — it surfaces only as a `drift` + `critical` inventory issue in the `repository-inventory:<name>` namespace ([#21](https://github.com/vig-os/org-config/issues/21)) and is then removed by hand. This binds today rather than being theoretical: the org has three members, of whom one is a plain member. Organization **owners** can always create repositories — GitHub grants that unconditionally and no org setting revokes it — so for them the config-first rule remains discipline backed by the drift sweep. That residual is accepted, not overlooked.
- **`devkit`'s `main` now dismisses stale approvals on push** ([#118](https://github.com/vig-os/org-config/issues/118)): `Main protection` required 1 approving review but left `dismiss_stale_reviews_on_push` off, so an approval survived every subsequent push to the PR branch — a reviewer could sign off on a two-line docs change and the author could then push arbitrary commits onto the same branch and merge to `main` on the strength of a review that never saw them. The required status checks are `strict` and re-run on every push, so CI could never be stale; the human review could, and after [#115](https://github.com/vig-os/org-config/issues/115) retired the unsatisfiable code-owner gate it is the only human control on `main`. `dismisses_stale_reviews: true` closes that window. `main` only: `Dev protection` and `Release protection` both require 0 approvals, so they have no review to dismiss.
- **CodeQL is now a required status check on `devkit`'s `main`** ([#115](https://github.com/vig-os/org-config/issues/115)): `devkit`'s `codeql.yml` already ran on every PR to `main`, but `Main protection` gated only on `15368:Test Summary`, so a PR with a red CodeQL leg merged. The ruleset now also requires `15368:CodeQL Analysis (actions)` and `15368:CodeQL Analysis (python)` — **two** contexts rather than one, because the workflow's `analyze` job is a `language: ['python', 'actions']` matrix and each leg reports under its own matrix-suffixed name (the single `CodeQL` check on the same commit comes from the `github-advanced-security` App, not from `github-actions`/`15368`, and is the code-scanning result check rather than a workflow job). `devkit` is public, so `codeql.yml`'s `if: ${{ !github.event.repository.private }}` guard never skips the job and neither context can hang pending. `Dev protection` and `Release protection` are deliberately unchanged — `main` is the branch releases are cut from.
- **`devkit` secret-scanning extras: found unsettable, not changed** ([#115](https://github.com/vig-os/org-config/issues/115)): `security_and_analysis.secret_scanning_non_provider_patterns` and `secret_scanning_validity_checks` read `disabled` on `devkit` despite the repo being public with secret scanning and push protection on, and they **cannot be turned on from here**. otterdog 1.3.4 models only `secret_scanning` and `secret_scanning_push_protection` (`otterdog/models/repository.py`; the vendored `otterdog-defaults.libsonnet` exposes the same two), so neither can be declared — and the out-of-band route is a dead end too: `gh api -X PATCH repos/vig-os/devkit` returns `200 OK` and silently changes nothing, because `devkit` is attached to the **enforced** `vig-os` code-security configuration `237480` ("Github + Advanced QL allowed"), which already declares both `enabled` at org level. An enforced configuration overrides repo-level writes, so the org says enabled, the repo reads disabled, and no write from either side moves it — the features need a GHAS Secret Protection entitlement the org's plan does not carry. Nothing was changed; the org configuration is deliberately left alone (it governs every repo in the org, and its `code_scanning_default_setup: enabled` interacts with `devkit`'s advanced CodeQL workflow). Recorded in [#116](https://github.com/vig-os/org-config/issues/116) as an **asserted** row carrying a `tolerated` value — `expect = "enabled"` (the desired state, on record) with `tolerated = "disabled"` (today's unsettable reality), so the row is clean-but-noted now and goes green by itself if the entitlement ever lands, rather than being silenced in `drift-allowlist.toml`. The same shape covers `sha_pinning_required` (true on `devkit`, false org-wide, unmodelled by otterdog: `expect = true`, `tolerated = false`); the fork-PR approval policy is asserted outright against `/orgs/{org}/actions/permissions/fork-pr-contributor-approval`, which does resolve. No row in the shipped `unmanaged-controls.toml` uses `kind = "unassertable"` — the escape hatch exists in the schema but nothing needed it.

## [v1.0.1](https://github.com/vig-os/org-config/releases/tag/v1.0.1) - 2026-08-07

### Added

- **House defaults overlay: a single source for the repository merge policy** ([#108](https://github.com/vig-os/org-config/issues/108)): new `otterdog/vig-os/house-defaults.libsonnet` re-exports the vendored Eclipse base template with the house merge policy (`allow_merge_commit: true`, rebase/squash off, `merge_commit_title: 'PR_TITLE'`, `merge_commit_message: 'PR_BODY'`) folded into `newRepo`, so every repo an org declares inherits it by construction instead of restating five fields. The policy previously had no single source: it was hand-copied onto individual repo blocks (`allow_merge_commit: true` appeared 12 times, the full five-field policy five times) while the vendored base declared the exact opposite, so any newly declared repo silently started on the upstream Eclipse defaults. The overlay resolves `vendor/…` relative to itself and names no org, so it is a single file dropped beside a downstream org's own vendored tree — `template/otterdog/YOUR_ORG/house-defaults.libsonnet` now ships it, and the skeleton's layout note plus the onboarding runbook document the one-line import change (`local orgs = import 'house-defaults.libsonnet';`) and how to normalize an `otterdog import` against it. It also exports `houseMergePolicy`, `upstreamMergePolicy`, and `legacyMergePolicy` mixins so a repo that deliberately sits on another policy names it rather than restating fields. `otterdog/vig-os/vig-os.jsonnet` is deduplicated against the overlay as a **pure refactor** — the evaluated config is byte-identical before and after (verified with `jsonnet`), so `scitadel` and `tessera` keep their documented deviations and the repos that predate the policy carry an explicit `+ orgs.legacyMergePolicy` (narrowing them to the house policy is a live settings change, deliberately out of scope here).

### Fixed

- **`template/` retargeted from the retired `dev` branch to trunk `main`** ([#106](https://github.com/vig-os/org-config/issues/106)): the downstream skeleton shipped in `v1.0.0` still described the pre-trunk `dev` model that #63 retired, so an org copying it verbatim got a `plan` caller that never fires (nothing opens PRs against `dev`), an `apply` caller that never fires (nothing pushes to `dev`), a Renovate base-branch pattern matching no branch, and a runbook pointing at a branch that does not exist — silent no-ops rather than errors, the worst failure mode for a write-path workflow. All eight references are now `main`: `plan.yml`'s `on.pull_request.branches`, `apply.yml`'s `on.push.branches` and its `production` deployment-branch-policy note, `import.yml`'s commit-back instruction, `renovate.json`'s `baseBranchPatterns`, and the three `README.md` onboarding steps (open the PR, the `production` environment policy, and where `apply` runs from). The example engine pins in `plan.yml` are bumped to `v1.0.1`, the first release carrying a skeleton that matches the engine it calls. `exo-pet/org-config` had already hand-adapted each of these twice (exo-pet/org-config#6, exo-pet/org-config#10).

## [v1.0.0](https://github.com/vig-os/org-config/releases/tag/v1.0.0) - 2026-08-07

### Added

- **Change-request intake: issue form, `change-request` label, and change-flow docs** ([#70](https://github.com/vig-os/org-config/issues/70)): adds a repo-owned `.github/ISSUE_TEMPLATE/change-request.yml` GitHub issue **form** for requesting a change to any managed org/repo setting (target repo(s) or org-wide, setting-area dropdown, current behavior, desired change, required justification, urgency) — explicitly marked NOT devkit-managed (it is not regenerated on a devkit upgrade, unlike the sibling forms), and auto-applying a new `change-request` label. Because `.github/label-taxonomy.toml` is devkit-managed, the label was **not** added there; it is created live on `vig-os/org-config` via `gh label create` (the form is deliberately **not** distributed to other repos — change requests are filed centrally in `org-config`, where setting changes are made, mirroring ADR-0002's centralized drift issues; and because the label lives outside the devkit-managed taxonomy, a `setup-labels --prune` run would delete it — a prune-safe repo-local taxonomy extension is requested upstream in [vig-os/devkit#1254](https://github.com/vig-os/devkit/issues/1254)). The downstream `template/.github/ISSUE_TEMPLATE/` skeleton (previously shipping no issue templates) now ships the same form with downstream wording plus a `config.yml` (`blank_issues_enabled: false`), and the `template/README.md` onboarding manifest lists the new path. Root `README.md` gains a **Requesting a change** section (open a change-request issue → maintainer PRs the jsonnet → plan diff on PR → gated apply on merge → drift catches out-of-band edits) and its stale "tooling … is not yet implemented" paragraph is corrected to state that plan/apply/drift are live. Also fixes stale post-#63 branch-model wording (`dev` → `main`) in `template/README.repo.md` ("How changes flow") and `docs/runbooks/secrets.md`.
- **`org-config` ruleset parity: `#OrganizationAdmin` bypass on Main protection + org-standard `Signed commits` ruleset** ([#65](https://github.com/vig-os/org-config/issues/65)): `org-config`'s own **Main protection** ruleset required 1 approving + code-owner review with **no bypass actors**, which is unsatisfiable for a sole maintainer — a PR author cannot approve their own PR, so `c-vigo`'s PRs to `main` could never merge (e.g. PR #64). Following the `#61`/`#62` `devkit` precedent, an always-active `#OrganizationAdmin` bypass is now declared on Main protection while the review policy itself (count 1 + code owners) stays in force for future co-maintainers. Separately, `org-config` was the only ruleset-governed repo in the org lacking the bypass-free `~ALL` **Signed commits** ruleset (declared for `commit-action`, `devkit`, `devkit-smoke-test`, `meta`, `part-registry`, `sync-issues-action`); it is now declared here too, so `requires_commit_signatures` enforcement applies to every branch and every actor — signatures deliberately live in their own bypass-free ruleset so the Main-protection `#OrganizationAdmin` bypass cannot weaken them.
- **Template onboarding gaps from the exo-pet pilot** ([#55](https://github.com/vig-os/org-config/issues/55)) — closes six documentation/skeleton gaps the `exo-pet/org-config` pilot (#25) surfaced against `template/`: (1) ships `template/README.repo.md`, a real post-onboarding repo README, so the instantiated repo keeps a README instead of retaining (or deleting) the onboarding runbook — Step 1.2 now renames it to `README.md`; (2) ships a proprietary `template/LICENSE` placeholder plus a new **License** section clarifying the downstream config repo is all-rights-reserved and must NOT copy the engine's Apache-2.0 license; (3) ships `template/.gitignore` (otterdog `/.cache/`, age private keys, `.env`); (4) documents the tag-vs-SHA / digest-pinning decision in Step 1 (previously only the `renovate.json` `_sha_pinning_note` key); (5) ships a one-time `template/.github/workflows/import.yml` bootstrap workflow — a read-only `workflow_dispatch` that runs `otterdog import` and uploads the generated jsonnet as an artifact (never commits or mutates the org), wired into Step 4; (6) aligns the README Free-plan posture cross-reference with the `drift.yml` caveat (both now cite the `vig-os/org-config` drift-layer-vendoring follow-up to #24). Docs/skeleton only — no engine or reusable-workflow change. The `otterdog_version` caller input was already delivered by #57; the `is_template` marking by #59.
- **Reusable `workflow_call` interfaces + downstream template skeleton** ([#24](https://github.com/vig-os/org-config/issues/24)) — `plan.yml`, `apply.yml`, and `drift.yml` now ALSO expose `workflow_call` (callable cross-org because this repo is public) so a downstream org's private `org-config` repo, created from the new `template/` skeleton, can consume the engine pinned to an exact tag/SHA (ADR-0006). Each takes `org_github_id`/`config_repo` inputs (defaulting to `vig-os`/`org-config`) that parameterize the `otterdog` org arg, the App-token `owner`, the drift `--org`/`--config-jsonnet` path, and the narrowed issue-ops token scope; the App credentials (and `apply`'s optional `SOPS_AGE_KEY`) are declared as `workflow_call` secrets **named identically to this repo's bootstrap Actions secrets**, so every credential reference is byte-identical whether reached on this repo's own events or via a call. Byte-compatibility for vig-os's own triggers is total: on a non-`workflow_call` event the `inputs` context is empty, so each `inputs.X || 'default'` resolves to the vig-os value and each credential falls through to the repo secret unchanged; the `owner`/org selectors resolve to `vig-os`; the `if:` repo-identity guards generalize from the `vig-os/org-config` literal to `format('{0}/{1}', …)` with the same truth value; and the concurrency groups gain an org suffix that is EMPTY on this repo's events (`plan`'s per-PR group and the shared `otterdog-mutate` mutation group stay literally unchanged, so `drift.yml`/`testbed-e2e.yml` keep sharing it) while a downstream caller serializes under `otterdog-mutate-<org>`. Org selectors flow into `run:` blocks via `env:` only (never inline `${{ }}`), keeping the zizmor template-injection surface closed. New `template/` skeleton (inert as live config — see below): a minimal placeholder `otterdog.json`, an `otterdog/README.md` layout note for `otterdog/<org>/<org>.jsonnet`, thin ~5-line caller workflows for plan/apply/drift with a clearly-marked `@vX.Y.Z` pin to replace (and a SHA-pin option noted for medtech), a `.sops.yaml` placeholder, a `drift-allowlist.toml` starter, a CODEOWNERS placeholder, a `renovate.json` that bumps the engine pin, and a `README.md` onboarding runbook (create private repo from template -> install App -> set 3 secrets -> import -> plan-first/read-only posture for Free orgs -> apply after the Team upgrade). `template/` is not picked up by this repo's own CI/hooks as live config: the L0 `validate` recipe's `find otterdog` and the `jsonnetfmt` hook's `^otterdog/` glob are root-anchored (never reach `template/otterdog/`); `actionlint` and `zizmor` scan only root `.github/workflows` (the template callers under `template/.github/workflows/` are never linted as live workflows); and the plan/apply workflows' `paths:` filters (`otterdog.json`, `otterdog/**`, `secrets/**`, `.sops.yaml`) are root-anchored so template copies never trigger a run. Marking the repo as a GitHub template is a one-time `gh` API action performed separately (out of scope for this change).
- **Inventory sweep: undeclared repos + `managed: false` allow-list** ([#21](https://github.com/vig-os/org-config/issues/21)): `src/drift_layer/inventory.py` compares the live org repo inventory (injected client, `GET /orgs/{org}/repos`) against the declared set extracted from the committed otterdog config — a pure, offline, literal read of every `orgs.newRepo('<name>')` (verified equal to a full go-jsonnet evaluation of the same file; keeps the L1 core native-lib-free). A live-but-undeclared repo and a declared-but-absent repo each become a `DriftRecord` (fingerprint namespaces `repository-inventory:<name>` / `repository-inventory-missing:<name>`, distinct titles) feeding the SAME dedup'd `drift`+`critical` reconcile/issue lifecycle (ADR-0002), on an `inventory`-labelled issue population. `drift-allowlist.toml` gains a `[[unmanaged]]` section (the `managed: false` allow-list): repos named there stay visible/reviewed but are exempt from undeclared-repo and settings-reconciliation flagging in both directions. Wired into the `drift-layer` CLI and `drift.yml` as a leg after the plan leg (org-repo read on the full plan token, issue writes on the `issues:write`-narrowed token); a sweep failure degrades explicitly in-tool — plan-drift reconciliation still runs and inventory issues are left untouched (never phantom-closed). Also fixes Python ignores: `__pycache__/`/caches now sit in the git-read root `.gitignore` (devkit only folds `.gitignore.project` in on regeneration).
- **Secrets as code: SOPS/age encrypt/apply pipeline** ([#22](https://github.com/vig-os/org-config/issues/22)) — `.sops.yaml` creation rules encrypt `secrets/*.yaml` (values only; keys stay reviewable) to the `vig-os` age recipient, with header-documented rotation (re-encrypt via PR + re-apply; `sops updatekeys` for key rotation) and the ADR-0003 bootstrap exclusions (the App private key and the age private key are never SOPS-managed). `secrets/vig-os.yaml` commits one canary (`ORG_CONFIG_CANARY`) as age ciphertext. `apply.yml` gains an in-memory decrypt-at-apply bridge (triggered by `secrets/**`/`.sops.yaml` changes): a pinned checksum-verified `sops` decrypts each value and loads it into an ephemeral, passphrase-less GPG-backed `pass` store under `runner.temp`, which otterdog resolves for any secret declared `value: 'pass:org-config/<NAME>'` — its only offline secret-value provider (the `env` provider raises on values). Values are `::add-mask::`-ed and never echoed; the GPG key is transport-only and adds no bootstrap secret. New `docs/runbooks/secrets.md` covers creation, rotation, and what never goes here.
- **Testbed repo + L3 mutation E2E harness** ([#23](https://github.com/vig-os/org-config/issues/23)) — declares the sacrificial public `org-config-testbed` repo in `otterdog/vig-os/vig-os.jsonnet` (description-only override; the vendored `newRepo` seeds no lists, so the evaluated config is drift-free-by-construction) and adds `.github/workflows/testbed-e2e.yml`: a scheduled (weekly Mon 05:23 UTC, away from drift's 03:17) + `workflow_dispatch` harness that joins the `otterdog-mutate` concurrency group (`cancel-in-progress: false`) and dogfoods the real drift path (ADR-0007 L3, never per-PR). It skips gracefully until the first apply creates the testbed, then induces out-of-band description drift via a full App token, runs the in-house `drift-layer`, and asserts exactly one `drift`+`critical` issue → update-not-duplicate on rerun → auto-close on revert, with an always-run idempotent cleanup and a PASS/FAIL summary carrying per-assertion evidence. An opt-in (default-OFF) stray-repo leg create/deletes `org-config-testbed-stray` and asserts the sweep issue, degrading gracefully until the sweep (#21) is deployed; repo deletion is covered by the App's Administration:write.
- **Strict-drift layer + scheduled reconciliation** ([#20](https://github.com/vig-os/org-config/issues/20))
  - `src/drift_layer/`: the in-house strict-drift core (ADR-0002/0007) — a pure-function pipeline with the GitHub client injected at the edge. `parser.py` turns `otterdog plan --local --no-web-ui` output into one `DriftRecord` per resource-level divergence (owning otterdog's `+`/`~`/`!`/`-` block syntax and the `Plan: X to add, Y to change, Z to delete` trailer, tolerating the uv/runner preamble); `allowlist.py` marks config-driven expected drift (`drift-allowlist.toml`) so known artifacts never open issues; `reconcile.py` diffs records against live issues into open/update/close actions; `github_client.py` is the stdlib-only REST edge (a `GitHubClient` Protocol tests fake).
  - Lifecycle (ADR-0002): exactly one deduplicated `drift`+`critical` issue per divergence, keyed by a stable per-resource fingerprint in a hidden `<!-- drift-fingerprint: … -->` marker; recurrence updates the existing issue in place (never duplicates) and adds a timestamped comment; resolution closes it with a comment. Title convention `Drift: <resource>`.
  - `drift-layer` CLI (`uv run drift-layer` / `python -m drift_layer`) with `--dry-run`; issues token read from `$GITHUB_TOKEN`.
  - `.github/workflows/drift.yml`: scheduled (daily 03:17 UTC) + `workflow_dispatch`; joins the static `otterdog-mutate` concurrency group (`cancel-in-progress: false`) so drift and apply never race; full org-wide App token for the read-only plan leg, a second `issues: write`-narrowed token for the issue-ops leg (ADR-0004/0007 least-privilege).
  - TDD: `tests/` with recorded plan fixtures (PR #41 ECA drift / 17 changes, PR #42 vs-dolt-only, synthetic empty and mixed-symbols plans) drives the L1 core. Repo becomes a `uv` project: `pyproject.toml` (`org-config-drift`, `requires-python >=3.12`, ruff + pytest, `src/` layout) + `uv.lock`; the `justfile.project` pytest/lint recipes activate.
- **Apply-on-merge workflow** ([#19](https://github.com/vig-os/org-config/issues/19))
  - `.github/workflows/apply.yml` runs a mutating `otterdog apply --local --no-web-ui --force` of the committed config against live `vig-os` on `push` to `dev` touching `otterdog.json`/`otterdog/**` (engine/workflow edits carry no config delta and are excluded from `paths:`), plus `workflow_dispatch` for reruns (ADR-0007 split cadence: config applies from `dev` on merge).
  - The job runs in the `production` environment (required reviewer `c-vigo`, `dev`-only deployment branch policy): entering it pauses the run until the deployment is approved — the designed human gate before any write token touches live `vig-os`. `push` fires only on this repo (never a fork), so org-admin credentials are never exposed to fork code; an explicit `github.repository` guard asserts this.
  - A static `otterdog-mutate` concurrency group with `cancel-in-progress: false` serializes all mutations without cancelling mid-apply; the future scheduled drift workflow (#20/#21) must reuse the same group so apply-vs-drift never races.
  - Uses the full App installation token (ADR-0004 Corrections) with two scoped, justified `github-app` inline zizmor ignores; least-privilege `permissions: contents: read`; `persist-credentials: false` checkout.
  - `--force` is mandatory for unattended apply (otterdog 1.3.4 prompts and blocks on stdin otherwise); `--local` gates only the vendor re-fetch (same `_init_base_template` skip as `plan`/`validate`), so apply evaluates the exact committed tree; `--delete-resources` is omitted so absent resources are never deleted. Reports otterdog's own apply output to the job summary (truncated safely); a nonzero exit is a failed/partial mutation and reddens the job (unlike plan, mutation errors are not benign report content).
- **Plan-on-PR workflow (L2)** ([#18](https://github.com/vig-os/org-config/issues/18))
  - `.github/workflows/plan.yml` runs a read-only `otterdog plan --local --no-web-ui` of the committed config against live `vig-os` on same-repo PRs touching `otterdog.json`/`otterdog/**`/the workflow, plus `workflow_dispatch`; forks are excluded per the ADR-0007 same-repo credential guard.
  - `--local` skips otterdog's `_init_base_template` vendor re-fetch, so plan evaluates the exact committed `otterdog/vig-os/vendor/` tree that L0 `otterdog validate --local` checks (no upstream EclipseFdn hooks re-introduced).
  - Uses the full App installation token (ADR-0004 Corrections) with two scoped, justified `github-app` inline zizmor ignores; least-privilege `permissions:` (`contents: read`, `pull-requests: write`); per-PR concurrency with cancel-in-progress.
  - Posts the plan as a marker-keyed PR comment updated in place (truncated safely, with run link) and to the job summary; the job fails only on validation/auth/harness errors — drift (exit 0) is report content, not failure. A fixed footnote flags the expected benign `vs-dolt` `code_scanning_default_languages` drift.
- **Import `vig-os` Otterdog configuration** ([#17](https://github.com/vig-os/org-config/issues/17))
  - `otterdog.json` (repo root) declares the `vig-os` org, `org-config` config repo, the `env` credential provider (`OTTERDOG_TOKEN` + dummy web-UI credential keys, unused on the App-token path), the pinned `EclipseFdn/otterdog-defaults@v0.13.1` base template, and `config_dir: otterdog` so all config lives under `otterdog/` (ADR-0006 dogfooding).
  - `otterdog/vig-os/vig-os.jsonnet`: normalized org config imported from live `vig-os`, declaring all 16 repos including the private `qms` (ADR-0006), plus org settings, secrets, rulesets, environments, and the `type` custom property. Drift-free against live so #18's first `plan` shows an empty diff; the 12 web-UI-only org settings stay unmanaged (spike #16).
  - Fixes the imported-config evaluation bug (#16): the `type` custom property used an inherited-`null` `default_value+:` merge (`null + array`); expressed as a plain `default_value:` override so evaluation succeeds.
  - Normalizes the base template's Eclipse-Foundation defaults out of vig-os state (they would otherwise be phantom drift): overrides `custom_properties`, `_repositories`, `security_managers`, and `teams` with `:`/`::` instead of the import's `+:`/`+::` appends, dropping the inherited `eclipse_project` org property, the `.eclipsefdn` example repo, the Eclipse security-manager teams, and the default org teams. vs-dolt's code-scanning languages are collapsed to the schema-valid `javascript-typescript`.
  - `otterdog/vig-os/vendor/otterdog-defaults/` vendors the base-template libsonnet (jsonnetfmt-formatted) so `otterdog validate --local` runs offline; the Eclipse-Foundation-specific Python validation/PMI hooks are omitted (they enforce Eclipse-only org policy).
  - Dev shell seeds a placeholder `OTTERDOG_TOKEN` (`flake.nix` `shellHook`) so the guarded `just validate` (jsonnetfmt + `otterdog validate --local`) is green locally; `/otterdog.json` added to `CODEOWNERS`.
- **Otterdog toolchain integration (L0)** ([#15](https://github.com/vig-os/org-config/issues/15))
  - Dev-shell (`flake.nix` `extraPackages`) gains `sops`, `age`, `go-jsonnet` (jsonnetfmt), `actionlint`, and `zizmor` (ADR-0005); otterdog itself runs via pinned `uvx` so CI and downstream template repos stay flake-independent.
  - Flake-generated pre-commit hooks add `jsonnetfmt --test` (otterdog jsonnet, no-op until #17) and `actionlint` (workflows).
  - `justfile.project` `validate` recipe (new "L0 validation" group) runs `actionlint` + `zizmor` over `.github/workflows` and, guarded on config presence, `jsonnetfmt --test` and `otterdog validate --local`; the otterdog version pin is a single `otterdog_version` variable (ADR-0007).
  - `zizmor.yml` baselines the devkit-managed workflows (per-audit, per-file) so `just validate` gates only org-config-authored workflows.
- **Architecture decision records 0001–0007** ([#1](https://github.com/vig-os/org-config/issues/1), [#8](https://github.com/vig-os/org-config/issues/8)–[#14](https://github.com/vig-os/org-config/issues/14))
  - `docs/adr/` with the house ADR convention (exo-fleet numbering, EXOPET vault template format) and index: 0001 reconciliation engine (Otterdog), 0002 drift semantics, 0003 secrets backend (SOPS/age), 0004 auth model (GitHub App), 0005 repo tooling, 0006 distribution topology & versioning, 0007 CI & testing strategy.
- **GitHub App runbook** ([#5](https://github.com/vig-os/org-config/issues/5)): `docs/runbooks/github-app.md` — creation, permissions table, bootstrap-secret handling, key rotation, downstream-org installation.
- **Adopt vigOS devkit 1.3.1** ([#2](https://github.com/vig-os/org-config/issues/2))
  - Greenfield scaffold of the shared vigOS dev environment in `direnv` mode: `flake.nix` + `.envrc` dev-shell, layered `justfile`s, managed pre-commit and `.github/` CI, and the `.vig-os` project manifest.
  - Pins move in lockstep: `.vig-os` `DEVKIT_VERSION=1.3.1`, `flake.nix` `vigos.url = "github:vig-os/devkit?ref=1.3.1"`, `flake.lock` locked to the 1.3.1 revision.
  - No language template and no release tag scheme yet (`DEVKIT_TAG_PREFIX`/`DEVKIT_FLOATING_TAGS` unset); the managed `codeql.yml` degrades to the language-less case (`actions` leg only, workflows-only push paths, vig-os/devkit#1142). Release workflows ship dormant.
  - Pre-commit hooks are flake-generated (`hooks = { }` in `flake.nix`, vig-os/devkit#883), matching the sibling direnv consumers: `.pre-commit-config.yaml` is a generated `/nix/store` symlink (gitignored) and the upstream `pymarkdown` hook is dropped (its `pyjson5` native dep cannot load on the host-runner direnv CI).

### Changed

- **Rewrite the root `README.md` to describe the shipped v1.0.0 engine** ([#26](https://github.com/vig-os/org-config/issues/26)): the README described the repo only as `vig-os`'s own config store, with the GitOps tooling summarized in a single paragraph and no mention of the engine/consumer split that M4 shipped. It now documents what v1.0.0 actually is: the Otterdog reconciliation model and the pinned-version/vendored-template layout (ADR-0001/ADR-0005); the three repo-owned, `workflow_call`-reusable workflows (`plan` on PR, `production`-gated `apply` on merge to `main`, daily `drift`) and the shared `otterdog-mutate` concurrency group (ADR-0007); the human gate on every mutation; issue-only drift semantics with the open/update/auto-close lifecycle, the inventory sweep, and the two-section allow-list (ADR-0002); SOPS/age secrets (ADR-0003); the least-privilege App auth model (ADR-0004); the four testing layers incl. the weekly destructive testbed E2E; downstream distribution via the private-repo `template/` skeleton pinned to a release tag/SHA, with the `exo-pet` pilot and the Free-plan read-only-first posture (ADR-0006); the trunk model on `main`; and a new **Known limitations** section covering the permanent one-line `vs-dolt` plan artifact ([upstream #694](https://github.com/eclipse-csi/otterdog/issues/694)), the otterdog ruleset-write bug that forces manual `gh api` ruleset changes ([#69](https://github.com/vig-os/org-config/issues/69), [upstream #695](https://github.com/eclipse-csi/otterdog/issues/695)), and the downstream drift-layer vendoring gap. Documentation only — no engine, config, or workflow change.
- **Retarget the reconciler engine and deployment gate from `dev` to trunk `main`** ([#67](https://github.com/vig-os/org-config/issues/67)): #63 (PR #64) switches this repo from the gitflow (`dev` → `main`) model to the trunk model — `main` becomes the single applied-state branch and the `dev` branch is deleted — but PR #64 deliberately left the reconciler engine untouched, leaving five `dev`-couplings that would render the state machine inert. All five are now retargeted to `main`: (1) `apply.yml`'s `push` trigger (config merges to `main` now apply); (2) `plan.yml`'s `pull_request` trigger (PRs to `main` now get a plan — note this migration PR itself targets `dev` and so gets no plan comment, which is accepted); (3) `drift.yml` drops its `actions/checkout` `ref: dev` pin (which would hard-fail the nightly scan once `dev` is deleted); (4) `testbed-e2e.yml` drops the same pin; (5) in `otterdog/vig-os/vig-os.jsonnet` (`orgs.newRepo('org-config')`), the `production` environment `branch_policies` entry `dev` → `main` (so an apply from `main` is no longer rejected at the environment gate) and the now-dead `Dev protection` ruleset (`refs/heads/dev`) is removed (`Main protection` and `Signed commits` remain). Each workflow's load-bearing header comments are rewritten from the gitflow rationale to the trunk model: under trunk, `main` is both where GitHub registers `schedule`/`workflow_dispatch` and where the config/code live, so the default `actions/checkout` ref is correct and the old `ref: dev` pin (which bridged a released-`main` workflow to dev-resident config) is obsolete; the `workflow_call`/downstream-caller story is unchanged. Two sequencing caveats are encoded: **(a)** `apply.yml`'s `push` trigger keeps `dev` alongside `main` (`branches: [main, dev]`) ONLY so this migration PR's own merge to `dev` still fires the LAST dev-based apply — the one that flips the environment policy to `main` — since a push-triggered workflow uses the workflow file at the pushed ref; the `dev` entry is inert once `dev` is deleted. **(b)** Removing the `Dev protection` ruleset from the jsonnet does NOT delete it live: apply deliberately omits `--delete-resources`, so resources absent from the config are never deleted. The live `Dev protection` ruleset (which does not set `allows_deletions` and would otherwise block the deletion) must be deleted **manually in the GitHub UI** before the `dev` branch is deleted.
- **Switch to the trunk workflow model** ([#63](https://github.com/vig-os/org-config/issues/63))
  - Re-scaffolds the managed dev-environment layer with `DEVKIT_WORKFLOW=trunk` (devkit 1.4.0, [vig-os/devkit#1205](https://github.com/vig-os/devkit/issues/1205)): `feature`/`bugfix`/`chore` branches now merge straight to `main`, releases fork `release/X.Y.Z` from `main` and merge back into `main`, and the long-lived `dev` branch is retired. As a desired-state config repo, `main` is the applied state; the gitflow `dev` buffer had left the org's entire configuration stranded on `dev` (68 commits ahead of a near-empty `main`) with no release train to drain it.
  - Trunk render (scaffold-time only, no runtime workflow logic): `.vig-os` gains `DEVKIT_WORKFLOW=trunk`; `sync-main-to-dev.yml` is removed; `ci.yml`/`codeql.yml` drop the `dev` PR trigger and `ci.yml`'s range-exclude trunk becomes `main`; `prepare-release.yml` and `sync-issues.yml` retarget `dev` → `main`; the `branch-naming` skill bases branches on `main`. The reconciler engine (`plan`/`apply`/`drift`/`testbed-e2e` workflows, otterdog config, `src/`) is untouched.
  - This PR lands the previously-stranded `dev` history on `main` in the same merge; the orphaned remote `dev` branch is deleted manually afterwards (the scaffold never deletes branches).
- **Upgrade vigOS devkit to 1.4.0** ([#53](https://github.com/vig-os/org-config/issues/53))
  - Re-scaffolds the managed dev-environment layer at devkit 1.4.0 in `direnv` mode (identity, `direnv`/`v` tag-prefix/no-floating-tags settings preserved). The scaffold/image pin is `.vig-os` `DEVKIT_VERSION=1.4.0`; the `flake.nix` `vigos` input floats on the devkit default branch (`vigos.url = "github:vig-os/devkit"`, per the documented NIX.md policy), with `flake.lock` relocked to its current revision.
  - Managed workflows are now zizmor-audited with the devkit-owned `zizmor.yml` baseline replacing the repo-local one; the project's own `plan.yml`/`apply.yml`/`drift.yml`/`testbed-e2e.yml` are untouched and stay gated on their inline ignores. Adds the `justfile` `with-native-libs` helper (vig-os/devkit#1181) and the `.vig-os` `DEVKIT_CI_RUNNER` runner knob (vig-os/devkit#1173).
  - The managed `.gitignore` now folds in the Python fragment: devkit 1.4.0 detects the repo's `pyproject.toml` (the drift-layer package) and ignores Python build/cache artifacts. No language template is applied and no project files (`pyproject.toml`, `src/`, `tests/`) are modified.
  - **Final adoption**: supersedes the `1.4.0-rc6` release-candidate validation pin with the released devkit `1.4.0`.
- **Harden org-config self-protection** ([#4](https://github.com/vig-os/org-config/issues/4))
  - Release tags now carry the `v` prefix (`DEVKIT_TAG_PREFIX=v`, actions-ecosystem convention; dormant until the first release).
  - `.github/CODEOWNERS` now gates admin-critical paths (`/.github/`, `/otterdog/`, `/secrets/`, `flake.nix`, `flake.lock`, `.vig-os`); `docs/` is intentionally left unowned so ADR/doc PRs to `dev` stay unblocked.

### Removed

- **Adopt the intentional deletion of the `meta`, `os-config`, and `part-registry` repos** ([#99](https://github.com/vig-os/org-config/issues/99), [#100](https://github.com/vig-os/org-config/issues/100), [#101](https://github.com/vig-os/org-config/issues/101)): the three repos were deleted from the live `vig-os` org out-of-band, so the committed config declared repositories that no longer exist. The inventory sweep opened one `Declared repository absent from org` issue per repo (#99–#101) and the plan leg opened eight `add` drift issues (#91, #97, #98 for the repositories themselves, plus #92–#96 for the five nested rulesets — `Main protection`/`Signed commits` on `meta` and `Dev protection`/`Main protection`/`Signed commits` on `part-registry`). The deletions are confirmed intentional, so the three `orgs.newRepo(...)` blocks — including their nested rulesets — are removed from `otterdog/vig-os/vig-os.jsonnet`, reconciling both drift populations back to an empty diff (the only remaining plan line is the allowlisted benign `vs-dolt` `code_scanning_default_languages` artifact). No live mutation is implied: apply runs without `--delete-resources`, and after this change the plan has nothing to add.

### Fixed

- **Declare `tessera`'s live `dev` branch protection and repo-scoped sync-App secrets** ([#87](https://github.com/vig-os/org-config/issues/87)): the drift sweep flagged three deletes on `tessera` — `branch_protection_rule[pattern="dev"]` (#87) and the `APP_SYNC_ISSUES_ID`/`APP_SYNC_ISSUES_PRIVATE_KEY` repo secrets (#88/#86) — all created out-of-band and never declared. All three are legitimate: the secrets hold the credentials of the **repo-scoped** `tessera-sync-issues-bot` GitHub App (app_id 4483218, installed 2026-08-04), a distinct App identity from the org-wide sync-issues App despite the shared secret names, and the `dev` rule guards tessera's active integration branch. `otterdog/vig-os/vig-os.jsonnet` now declares them (secret values `'********'`, managed out-of-band), so the plan stops proposing deletions that would break tessera's issue-sync workflow and drop a live branch gate. The `dev` rule's `nix flake check` is declared **without** the `any:` prefix (unlike `main`'s), matching otterdog's serialization of the live app-bound check (app_id 15368).
- **Declare the intentional `#OrganizationAdmin` bypass on the three `devkit` rulesets** ([#61](https://github.com/vig-os/org-config/issues/61)): an org owner added an always-active `#OrganizationAdmin` bypass actor to `devkit`'s Dev/Main/Release protection rulesets out-of-band, so every plan showed a spurious `bypass_actors` change on all three. The bypass is confirmed intentional, so `otterdog/vig-os/vig-os.jsonnet` now declares it (added to Dev/Release alongside `commit-action-bot`, and as the sole bypass on Main), reconciling the drift to an empty diff.
- **Reusable workflows are now actually callable by downstream orgs: otterdog version pin via input** ([#56](https://github.com/vig-os/org-config/issues/56)): the exo-pet pilot's first real `workflow_call` invocation (exo-pet/org-config#3) failed in the "Resolve otterdog version pin" step — it greps `justfile.project`, which only this engine repo has (the `template/` skeleton ships none), so `plan`/`apply`/`drift` were unusable downstream. All three now accept an optional `otterdog_version` `workflow_call` input; resolution order is input → `justfile.project` → explicit error. This repo's own events keep sourcing the pin from `justfile.project` (ADR-0005 SSoT, empty input); downstream callers pass their own pin, kept beside the engine SHA pin, and the `template/` callers now do so with a lockstep note. Also de-hardcodes vig-os from the downstream-visible surface of `plan`: the job name, the report/comment headers (now the caller's `org_github_id`), and the vig-os-specific `vs-dolt` footnote (emitted only when planning vig-os) — a downstream plan comment previously claimed to be a vig-os plan.
- **Declare `is_template: true` for `org-config` in the committed vig-os config** ([#58](https://github.com/vig-os/org-config/issues/58)): marking this repo as a GitHub template for downstream instantiation (#52) was a one-time API action never reflected in `otterdog/vig-os/vig-os.jsonnet`, so every plan showed a second change beyond the baselined vs-dolt artifact — and an apply would have silently UN-marked the template, breaking downstream org-config creation.
- **Scheduled workflows check out trunk `dev` so they operate on current config post-release** ([#23](https://github.com/vig-os/org-config/issues/23)): `drift.yml` and `testbed-e2e.yml` now pin `actions/checkout` to `ref: dev`. GitHub registers `schedule`/`workflow_dispatch` triggers only from the default branch (`main`, which advances only via a release), so both workflows are inert until the first release (v1.0.0) promotes them onto `main` — at which point the workflow FILE is main's (released) but the config, `src/drift_layer/` code, `drift-allowlist.toml`, and the `justfile.project` otterdog pin it reads all still live on `dev` between releases; without the explicit `ref` the run would take the `main` triggering ref and scan stale released config. Executing dev's Python from a main-pinned workflow is trust-equivalent to the existing dev-push `apply` (both gated by the same PR-to-`dev` review) and opens no new injection surface; each workflow header now documents the registration/activation model (dispatch also only works once the file is on `main`) and the checkout-ref rationale. `apply.yml`/`plan.yml` need no such fix — their `push`/`pull_request`-on-`dev` triggers make the triggering ref already `dev`. Also documents the repository-level **Custom properties: Read & write** App permission in `docs/runbooks/github-app.md`: org-level Custom properties covers only the schema, so setting repo custom-property VALUES (the `type` property) 403s without the repo-level grant — verified 2026-07-17 (apply run `29584038705`).
- **Normalize the base template's ECA status check out of all rulesets** ([#17](https://github.com/vig-os/org-config/issues/17)): the first live plan (PR #41, 17 changes) showed `+ "eclipse-eca-validation:eclipsefdn/eca"` on every ruleset with status checks — `newStatusChecks()`/`newBranchProtectionRule()` seed that Eclipse default and the imported config's `status_checks+:` appends inherited it (same class as the `custom_properties` `null + array` fix). All 15 ruleset `status_checks` lists and tessera's branch-protection `required_status_checks` are now plain overrides expressing live state exactly (cross-checked read-only against `gh api .../rulesets`); no other template-seeded injection remains (all other appended fields have empty template defaults). The only remaining plan line is the known benign `vs-dolt` `code_scanning_default_languages` artifact (otterdog's live read filters out `javascript-typescript` while its schema rejects the split values — upstream inconsistency, already baselined in plan.yml's footnote).

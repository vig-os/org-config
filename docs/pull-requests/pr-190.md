---
type: pull_request
state: closed (merged)
branch: feature/189-suppress-codeql-default-setup → main
created: 2026-08-21T20:49:39Z
updated: 2026-08-21T20:51:50Z
author: c-vigo
author_url: https://github.com/c-vigo
url: https://github.com/vig-os/org-config/pull/190
comments: 1
labels: none
assignees: none
milestone: none
projects: none
merged: 2026-08-21T20:51:47Z
synced: 2026-08-22T02:56:14.943Z
---

# [PR 190](https://github.com/vig-os/org-config/pull/190) chore(config): suppress CodeQL default setup org-wide

## Summary

Implements #189 — CodeQL **default setup** is retired; the devkit-scaffolded advanced `codeql.yml` (already on 7 of 11 repos) is the org's one scanning mechanism.

- `otterdog/vig-os/vig-os.jsonnet`: the three `code_scanning_default_setup_enabled: true` declarations (tessera, vigos-mvp, vs-dolt) and both `code_scanning_default_languages` lists are deleted; the vendored defaults (`false` / `[]`) now apply.
- `unmanaged-controls.toml`: new org-scope row asserting configuration `237480` › `code_scanning_default_setup = disabled`, so a UI re-enable raises a deduplicated drift issue instead of silently re-arming default setup fleet-wide.
- Retired three artifacts that existed only to paper over default-setup drift: the vs-dolt `[[expected]]` entry in `drift-allowlist.toml`, the vig-os benign-drift footnote machinery in `plan.yml`, and the matching README known-limitation (upstream [eclipse-csi/otterdog#694](https://github.com/eclipse-csi/otterdog/issues/694)). **A clean plan now reads all zeros.**

## Pre-step already executed (issue step 1)

`237480`'s code-scanning field was flipped via `gh api` on 2026-08-21 **before** this PR, per the ordering in #189 (enablement writes bounce off enforcement until the config allows them). Verified propagated: all three repos read `state: not-configured` live. The enforced configuration itself is untouched otherwise — its secret-scanning declarations remain load-bearing for the devkit rows in `unmanaged-controls.toml`.

## Verification

- `just precommit` fully green, otterdog validation 0 errors; `just test` 99 passed (controls/parser/reconcile suites cover the edited TOML files).
- Live control value confirmed: `GET /orgs/vig-os/code-security/configurations/237480` → `code_scanning_default_setup: disabled` — the new control row seeds green.
- **Expected plan on this PR: zero diffs** (the vs-dolt cosmetic diff is gone with the field). Apply-on-merge should be a no-op for repo settings since live already converged when 237480 flipped.

## Coverage after this change (explicit)

| Repo | State |
|---|---|
| tessera | unscanned **until** [vig-os/tessera#410](https://github.com/vig-os/tessera/issues/410) (actions-leg `codeql.yml`, unblocked by this PR) |
| vs-dolt | unscanned **by decision** |
| vigos-mvp | loses nothing — no CodeQL-supported code |

Closes #189

Refs: #189


---
---

# Comments (1)

## [Comment #1](https://github.com/vig-os/org-config/pull/190#issuecomment-5375263218) by [@github-actions[bot]](https://github.com/apps/github-actions)

_Posted on August 21, 2026 at 08:49 PM_

<!-- otterdog-plan-report -->
### Otterdog plan — `vig-os` (read-only)

_`plan --local --no-web-ui` against the committed config — the exact tree L0 `otterdog validate --local` checks. Nothing is applied._

Plan completed. A non-empty diff is drift for the PR author to decide on, **not** a failure.

<details><summary>Plan output</summary>

```text
Installed 53 packages in 60ms
Planning execution:

Actions are indicated with the following symbols:
  + create
  ~ modify
  ! forced update
  - delete

Project vig-os[github_id=vig-os] (1/1)
  there have been 31 validation infos, enable verbose output to display them.
  
  Plan: 0 to add, 0 to change, 0 to delete.
```

</details>

[Run log](https://github.com/vig-os/org-config/actions/runs/32525524221) · triggered by `pull_request`


---
---

## Commits

### Commit 1: [acda410](https://github.com/vig-os/org-config/commit/acda410c66ad968ea3edb64e4c2c7c039dcdd2be) by [c-vigo](https://github.com/c-vigo) on August 21, 2026 at 08:49 PM
chore(config): suppress CodeQL default setup org-wide, 70 files modified (.github/workflows/plan.yml, CHANGELOG.md, README.md, drift-allowlist.toml, otterdog/vig-os/vig-os.jsonnet, unmanaged-controls.toml)

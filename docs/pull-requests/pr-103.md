---
type: pull_request
state: closed (merged)
branch: bugfix/99-adopt-repo-deletions → main
created: 2026-08-07T08:49:28Z
updated: 2026-08-07T08:57:46Z
author: c-vigo
author_url: https://github.com/c-vigo
url: https://github.com/vig-os/org-config/pull/103
comments: 1
labels: none
assignees: none
milestone: none
projects: none
merged: 2026-08-07T08:57:44Z
synced: 2026-08-08T03:28:39.777Z
---

# [PR 103](https://github.com/vig-os/org-config/pull/103) fix(config): adopt removal of meta, os-config and part-registry

## Summary

The repositories `meta`, `os-config`, and `part-registry` were deleted from the
live `vig-os` org out-of-band. The deletion is **confirmed intentional**, so the
committed config is updated to match live: the three `orgs.newRepo(...)` blocks
are removed from `otterdog/vig-os/vig-os.jsonnet`, including the five nested
rulesets they carried.

## What changed

- `otterdog/vig-os/vig-os.jsonnet`: removed `orgs.newRepo('meta')` (with its
  `Main protection` and `Signed commits` rulesets), `orgs.newRepo('os-config')`,
  and `orgs.newRepo('part-registry')` (with its `Dev protection`,
  `Main protection`, and `Signed commits` rulesets).
- `CHANGELOG.md`: `## Unreleased` -> `### Removed` entry.

No other reference to the three repo names exists anywhere under `otterdog/` or
in `drift-allowlist.toml` (grepped repo-wide).

## Effect on the plan

This clears the 8 plan-drift `add` records (3 repositories + 5 rulesets) and the
3 inventory `declared repository absent` records. Once merged alongside #102 the
plan should read `0 to add, 1 to change, 0 to delete` — the single change being
the allowlisted benign `vs-dolt` `code_scanning_default_languages` artifact.

No live mutation is implied: apply runs without `--delete-resources`, and after
this change the plan has nothing to add.

## Tracking issues

Related drift issues (the drift layer auto-closes them once the divergence is
gone, so no closing keywords are used here): #91, #92, #93, #94, #95, #96, #97,
#98, #99, #100, #101.

Refs: #99



---
---

# Comments (1)

## [Comment #1](https://github.com/vig-os/org-config/pull/103#issuecomment-5214924843) by [@github-actions[bot]](https://github.com/apps/github-actions)

_Posted on August 7, 2026 at 08:49 AM_

<!-- otterdog-plan-report -->
### Otterdog plan — `vig-os` (read-only)

_`plan --local --no-web-ui` against the committed config — the exact tree L0 `otterdog validate --local` checks. Nothing is applied._

Plan completed. A non-empty diff is drift for the PR author to decide on, **not** a failure.

<details><summary>Plan output</summary>

```text
Downloading aiohttp (1.7MiB)
Downloading pygments (1.2MiB)
Downloading pynacl (1.3MiB)
Downloading playwright (45.5MiB)
Downloading rjsonnet (1.2MiB)
Downloading cryptography (4.5MiB)
 Downloaded rjsonnet
 Downloaded pynacl
 Downloaded aiohttp
 Downloaded cryptography
 Downloaded playwright
 Downloaded pygments
Installed 53 packages in 36ms
Planning execution:

Actions are indicated with the following symbols:
  + create
  ~ modify
  ! forced update
  - delete

Project vig-os[github_id=vig-os] (1/1)
  there have been 22 validation infos, enable verbose output to display them.

  
  ~ repository[name="vs-dolt"] {
    ~ code_scanning_default_languages = [
      ~ "javascript"          -> "javascript-typescript"
      - "typescript"
    ~ ]
  ~ }
  
  Plan: 0 to add, 1 to change, 0 to delete.
```

</details>

> **Expected benign drift — do not chase:** `vs-dolt` › `code_scanning_default_languages`
> always reports as changed. GitHub's import splits `javascript-typescript` into
> `[javascript, typescript]`, a form otterdog's own validator rejects, so the committed
> config keeps the schema-valid `javascript-typescript`. The diff is cosmetic and expected
> on every run.

[Run log](https://github.com/vig-os/org-config/actions/runs/31163736309) · triggered by `pull_request`


---
---

## Commits

### Commit 1: [1ec3f73](https://github.com/vig-os/org-config/commit/1ec3f736fb54caa44abeb2dd0182107031fd7f93) by [c-vigo](https://github.com/c-vigo) on August 7, 2026 at 08:49 AM
fix(config): adopt removal of meta, os-config and part-registry, 108 files modified (CHANGELOG.md, otterdog/vig-os/vig-os.jsonnet, tests/conftest.py)

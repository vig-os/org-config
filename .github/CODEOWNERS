# Seeded by vigOS devkit — yours to edit; upgrades never overwrite this file.
# Bugs / missing tools: https://github.com/vig-os/devkit/issues

# CODEOWNERS — default reviewers for pull requests
# Docs: https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/about-code-owners
#
# Each line defines a file pattern and the GitHub users/teams required to review
# changes to matching files. Later rules take precedence over earlier ones.

# Only admin-critical paths carry the code-owner gate (enforced on main): the
# org/CI/secrets/toolchain config whose compromise equals org admin control.
# `docs/` is deliberately left unowned so ADR/doc PRs to dev stay unblocked.
/.github/                        @c-vigo
/otterdog.json                   @c-vigo
/otterdog/                       @c-vigo
/secrets/                        @c-vigo
/flake.nix                       @c-vigo
/flake.lock                      @c-vigo
/.vig-os                         @c-vigo
